namespace SuperUI.Enums;

/// <summary>Pre-built skeleton layout templates.</summary>
public enum SgSkeletonVariant
{
    /// <summary>Default — no preset template; uses shape parameters.</summary>
    Default,

    /// <summary>Card-shaped skeleton with image area, title, and two paragraph lines.</summary>
    Card,

    /// <summary>Table-shaped skeleton with a header row and four data rows.</summary>
    Table,

    /// <summary>Dashboard-shaped skeleton with a 2x2 widget grid.</summary>
    Dashboard,

    /// <summary>Avatar list skeleton with avatar circles and text lines.</summary>
    AvatarList,

    /// <summary>Article-shaped skeleton with featured image, headline, and paragraph.</summary>
    Article
}
