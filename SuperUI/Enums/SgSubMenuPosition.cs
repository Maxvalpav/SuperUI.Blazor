namespace SuperUI.Enums;

/// <summary>Defines which side the sub-menu opens relative to the parent item.</summary>
public enum SgSubMenuPosition
{
    /// <summary>Sub-menu opens to the right (default).</summary>
    Right,
    /// <summary>Sub-menu opens to the left.</summary>
    Left
}
