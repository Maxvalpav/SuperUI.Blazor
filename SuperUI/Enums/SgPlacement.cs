namespace SuperUI.Enums;

/// <summary>Расположение оверлея относительно триггера.</summary>
public enum SgPlacement
{
    Top, Bottom, Left, Right,
    TopStart, TopEnd,
    BottomStart, BottomEnd,
    LeftStart, LeftEnd,
    RightStart, RightEnd
}
