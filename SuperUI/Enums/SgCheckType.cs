namespace SuperUI.Enums;

/// <summary>Defines the check style for a checkable menu item.</summary>
public enum SgCheckType
{
    /// <summary>Checkbox style — can be toggled on/off independently.</summary>
    Checkbox,
    /// <summary>Radio style — typically used in a group where only one can be checked.</summary>
    Radio
}
