namespace SuperUI.Enums;

/// <summary>Position of the loading spinner inside a button.</summary>
public enum SgButtonLoadingPosition
{
    /// <summary>Spinner replaces icon on the left of the text.</summary>
    Left,
    /// <summary>Spinner appears after the text on the right.</summary>
    Right,
    /// <summary>Spinner replaces both icon and text (centered).</summary>
    Center
}
