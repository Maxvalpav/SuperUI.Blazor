namespace SuperUI.Enums;

/// <summary>Ribbon-button display size.</summary>
public enum SgRibbonButtonSize
{
    /// <summary>Large (default) — icon + text, two rows.</summary>
    Large = 0,
    /// <summary>Medium — icon + text in one row.</summary>
    Medium = 1,
    /// <summary>Small — icon only, tooltip on hover.</summary>
    Small = 2
}
