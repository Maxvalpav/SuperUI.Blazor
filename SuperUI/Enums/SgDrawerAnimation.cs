namespace SuperUI.Enums;

/// <summary>Drawer open/close animation style.</summary>
public enum SgDrawerAnimation
{
    /// <summary>Slide in from the placement side (default).</summary>
    Slide,
    /// <summary>Fade in/out with no slide.</summary>
    Fade,
    /// <summary>Scale (zoom) in/out from center.</summary>
    Scale
}
