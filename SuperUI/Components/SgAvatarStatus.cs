namespace SuperUI.Components;

/// <summary>
/// Defines the status indicator options for the avatar component.
/// </summary>
public enum SgAvatarStatus
{
    /// <summary>No status indicator.</summary>
    None,
    /// <summary>Online status (green).</summary>
    Online,
    /// <summary>Offline status (gray).</summary>
    Offline,
    /// <summary>Busy status (red).</summary>
    Busy,
    /// <summary>Away status (yellow/orange).</summary>
    Away
}
