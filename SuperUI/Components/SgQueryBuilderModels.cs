namespace SuperUI.Components;

using SuperUI.Enums;

/// <summary>Optional pre-resolved enum members for select-based filter inputs.</summary>
public sealed record QueryFieldEnumOption(string Name, string Label);
