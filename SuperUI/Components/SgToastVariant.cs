namespace SuperUI.Components;

/// <summary>
/// Defines the visual variant options for toast notifications.
/// </summary>
public enum SgToastVariant
{
    /// <summary>Default neutral toast.</summary>
    Default,
    /// <summary>Success toast (green).</summary>
    Success,
    /// <summary>Danger / error toast (red).</summary>
    Danger,
    /// <summary>Warning toast (yellow/orange).</summary>
    Warn
}
