namespace SuperUI.Components;

/// <summary>
/// Defines the layout orientation for components such as <see cref="SgSplitter"/> and <see cref="SgStack"/>.
/// </summary>
public enum SgOrientation
{
    /// <summary>Side-by-side (left / right) layout.</summary>
    Horizontal,
    /// <summary>Stacked (top / bottom) layout.</summary>
    Vertical
}
