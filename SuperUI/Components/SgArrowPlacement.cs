namespace SuperUI.Components;

/// <summary>
/// Defines where the expand/collapse arrow is placed in the <see cref="SgCollapse"/> header.
/// </summary>
public enum SgArrowPlacement
{
    /// <summary>Arrow at the start (left) of the header.</summary>
    Start,
    /// <summary>Arrow at the end (right) of the header (default).</summary>
    End
}
