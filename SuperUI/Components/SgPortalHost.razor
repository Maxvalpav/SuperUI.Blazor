@namespace SuperUI.Components
@* SgPortalHost is not required — SgPortal teleports content directly to document.body.
   This stub exists only so that existing markup referencing <SgPortalHost /> compiles. *@
