@namespace SuperUI.Components

@* SgPortalHost: required host component for portal infrastructure.
   Place <SgPortalHost /> in MainLayout.razor alongside SgToastHost and SgConfirmHost.
   Renders no visual output — children are provided for future extensibility. *@
@ChildContent

@code {
    /// <summary>
    /// Content rendered by the portal host. Typically empty — reserved for future use.
    /// </summary>
    [Parameter] public RenderFragment? ChildContent { get; set; }
}
