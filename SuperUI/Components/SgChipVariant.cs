namespace SuperUI.Components;

/// <summary>
/// Defines the visual variant options for the <see cref="SgChip"/> component.
/// </summary>
public enum SgChipVariant
{
    /// <summary>Default neutral chip.</summary>
    Default,
    /// <summary>Primary chip (accent color).</summary>
    Primary,
    /// <summary>Success chip (green).</summary>
    Success,
    /// <summary>Danger chip (red).</summary>
    Danger,
    /// <summary>Warning chip (yellow/orange).</summary>
    Warn
}
