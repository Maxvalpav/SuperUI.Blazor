@page "/dropdown-demo"
@using SuperUI.Components
@using SuperUI.Enums
@using Microsoft.JSInterop
@inject IJSRuntime JS

<PageTitle>SgDropdown — Dropdown Menu — SuperUI</PageTitle>

<SgCard Variant="SgCardVariant.Default" Shadow="SgShadow.Md" Radius="SgSize.Lg"
        Status="SgCardStatus.Info" Hoverable="false"
        HeaderContent="@_mainHeader">
    <ChildContent>

    <!-- ═══════════════════════════════════════════════════════════════════ -->
    <!-- 1. Basic Usage                                                     -->
    <!-- ═══════════════════════════════════════════════════════════════════ -->
    <div class="demo-section">
        <div class="demo-section-header">
            <SgTypography Variant="SgTypographyVariant.Heading5" Weight="SgTypographyWeight.Semibold">
                Basic Usage
            </SgTypography>
            <SgTypography Size="SgTypographySize.Sm" Color="var(--sg-text-muted)">
                Simple dropdown with action items. Click the button to open, click an item to select.
            </SgTypography>
        </div>

        <div class="demo-two-col">
            <SgCard Title="Examples" Shadow="SgShadow.Sm" Size="SgSize.Sm">
                <SgStack Vertical Space="SgSize.Lg">
                    <div>
                        <SgTypography Size="SgTypographySize.Xs" Weight="SgTypographyWeight.Bold" Transform="SgTypographyTransform.Uppercase" Tracking="SgTypographyTracking.Wide" Color="var(--sg-text-muted)" Style="display:block;margin-bottom:10px;border-bottom:1px solid var(--sg-border);padding-bottom:4px;">
                            Actions Menu
                        </SgTypography>
                        <SgSpace>
                            <SgDropdown Text="Actions" ShowCaret="true">
                                <SgDropdownItem Text="View" OnClick="@LogView" />
                                <SgDropdownItem Text="Edit" OnClick="@LogEdit" />
                                <SgDropdownItem Text="Delete" Danger="true" OnClick="@LogDelete" />
                            </SgDropdown>
                            <SgDropdown Text="More" Variant="SgButtonVariant.Outlined" ShowCaret="true">
                                <SgDropdownItem Text="Duplicate" OnClick="@(() => Log("Duplicate"))" />
                                <SgDropdownItem Text="Move" OnClick="@(() => Log("Move"))" />
                                <SgDropdownItem Divider="true" />
                                <SgDropdownItem Text="Archive" OnClick="@(() => Log("Archive"))" />
                            </SgDropdown>
                        </SgSpace>
                    </div>
                    <div>
                        <SgTypography Size="SgTypographySize.Xs" Weight="SgTypographyWeight.Bold" Transform="SgTypographyTransform.Uppercase" Tracking="SgTypographyTracking.Wide" Color="var(--sg-text-muted)" Style="display:block;margin-bottom:10px;border-bottom:1px solid var(--sg-border);padding-bottom:4px;">
                            Event Log
                            @if (_logs.Count > 0)
                            {
                                <button type="button" class="demo-inline-btn" @onclick="ClearLogs" title="Clear log">✕</button>
                            }
                        </SgTypography>
                        <div class="demo-log">
                            @if (_logs.Count == 0)
                            {
                                <span class="demo-log-empty">Click a dropdown item to see events</span>
                            }
                            @foreach (var log in _logs)
                            {
                                <div class="demo-log-entry">@log</div>
                            }
                        </div>
                    </div>
                </SgStack>
            </SgCard>

            <SgCard Title="SgDropdown Properties" Shadow="SgShadow.Sm" Size="SgSize.Sm">
                <PropertyTable Items="_basicProperties" />
            </SgCard>
        </div>
    </div>

    <!-- ═══════════════════════════════════════════════════════════════════ -->
    <!-- 2. With Icons                                                      -->
    <!-- ═══════════════════════════════════════════════════════════════════ -->
    <div class="demo-section">
        <div class="demo-section-header">
            <SgTypography Variant="SgTypographyVariant.Heading5" Weight="SgTypographyWeight.Semibold">
                With Icons
            </SgTypography>
            <SgTypography Size="SgTypographySize.Sm" Color="var(--sg-text-muted)">
                Dropdown items with Heroicons leading icons via <SgTypography As="SgTypographyElement.Code" Size="SgTypographySize.Sm">Icon</SgTypography> parameter.
            </SgTypography>
        </div>

        <div class="demo-two-col">
            <SgCard Title="Examples" Shadow="SgShadow.Sm" Size="SgSize.Sm">
                <SgStack Vertical Space="SgSize.Lg">
                    <div>
                        <SgTypography Size="SgTypographySize.Xs" Weight="SgTypographyWeight.Bold" Transform="SgTypographyTransform.Uppercase" Tracking="SgTypographyTracking.Wide" Color="var(--sg-text-muted)" Style="display:block;margin-bottom:10px;border-bottom:1px solid var(--sg-border);padding-bottom:4px;">
                            User Settings
                        </SgTypography>
                        <SgDropdown Text="Settings" Variant="SgButtonVariant.Primary" ShowCaret="true">
                            <SgDropdownItem Text="Profile" Icon="@SgHeroicons.Outline.User" OnClick="@(() => Log("Profile"))" />
                            <SgDropdownItem Text="Security" Icon="@SgHeroicons.Outline.LockClosed" OnClick="@(() => Log("Security"))" />
                            <SgDropdownItem Text="Notifications" Icon="@SgHeroicons.Outline.Bell" OnClick="@(() => Log("Notifications"))" />
                            <SgDropdownItem Divider="true" />
                            <SgDropdownItem Text="Logout" Icon="@SgHeroicons.Outline.ArrowRightOnRectangle" Danger="true" OnClick="@(() => Log("Logout"))" />
                        </SgDropdown>
                    </div>
                    <div>
                        <SgTypography Size="SgTypographySize.Xs" Weight="SgTypographyWeight.Bold" Transform="SgTypographyTransform.Uppercase" Tracking="SgTypographyTracking.Wide" Color="var(--sg-text-muted)" Style="display:block;margin-bottom:10px;border-bottom:1px solid var(--sg-border);padding-bottom:4px;">
                            File Actions
                        </SgTypography>
                        <SgDropdown Text="File" Variant="SgButtonVariant.Outlined">
                            <SgDropdownItem Text="New" Icon="@SgHeroicons.Outline.DocumentPlus" OnClick="@(() => Log("New"))" />
                            <SgDropdownItem Text="Open" Icon="@SgHeroicons.Outline.FolderOpen" OnClick="@(() => Log("Open"))" />
                            <SgDropdownItem Text="Save" Icon="@SgHeroicons.Outline.ArrowDownOnSquare" Shortcut="⌘S" OnClick="@(() => Log("Save"))" />
                            <SgDropdownItem Divider="true" />
                            <SgDropdownItem Text="Export PDF" Icon="@SgHeroicons.Outline.DocumentArrowDown" OnClick="@(() => Log("Export PDF"))" />
                            <SgDropdownItem Text="Print" Icon="@SgHeroicons.Outline.Printer" Shortcut="⌘P" Disabled="true" OnClick="@(() => Log("Print"))" />
                        </SgDropdown>
                    </div>
                </SgStack>
            </SgCard>

            <SgCard Title="SgDropdownItem Properties" Shadow="SgShadow.Sm" Size="SgSize.Sm">
                <PropertyTable Items="_itemProperties" />
            </SgCard>
        </div>
    </div>

    <!-- ═══════════════════════════════════════════════════════════════════ -->
    <!-- 3. Custom Trigger                                                  -->
    <!-- ═══════════════════════════════════════════════════════════════════ -->
    <div class="demo-section">
        <div class="demo-section-header">
            <SgTypography Variant="SgTypographyVariant.Heading5" Weight="SgTypographyWeight.Semibold">
                Custom Trigger
            </SgTypography>
            <SgTypography Size="SgTypographySize.Sm" Color="var(--sg-text-muted)">
                Use any content as the dropdown trigger via <SgTypography As="SgTypographyElement.Code" Size="SgTypographySize.Sm">TriggerContent</SgTypography>.
            </SgTypography>
        </div>

        <div class="demo-two-col">
            <SgCard Title="Button Trigger" Shadow="SgShadow.Sm" Size="SgSize.Sm">
                <SgDropdown>
                    <TriggerContent>
                        <SgButton Variant="SgButtonVariant.Success" Text="Export Data" />
                    </TriggerContent>
                    <ChildContent>
                        <SgDropdownItem Text="Export CSV" Icon="@SgHeroicons.Outline.DocumentArrowDown" OnClick="@(() => Log("CSV"))" />
                        <SgDropdownItem Text="Export PDF" Icon="@SgHeroicons.Outline.DocumentText" OnClick="@(() => Log("PDF"))" />
                        <SgDropdownItem Text="Export XLSX" Icon="@SgHeroicons.Outline.TableCells" OnClick="@(() => Log("XLSX"))" />
                        <SgDropdownItem Divider="true" />
                        <SgDropdownItem Text="Print" Icon="@SgHeroicons.Outline.Printer" Disabled="true" OnClick="@(() => Log("Print"))" />
                    </ChildContent>
                </SgDropdown>
            </SgCard>
            <SgCard Title="Icon-only Trigger" Shadow="SgShadow.Sm" Size="SgSize.Sm">
                <SgDropdown>
                    <TriggerContent>
                        <SgButton Variant="SgButtonVariant.Ghost" Text="⚙" />
                    </TriggerContent>
                    <ChildContent>
                        <SgDropdownItem Text="Profile" Icon="@SgHeroicons.Outline.User" OnClick="@(() => Log("Profile"))" />
                        <SgDropdownItem Text="Settings" Icon="@SgHeroicons.Outline.Cog6Tooth" OnClick="@(() => Log("Settings"))" />
                        <SgDropdownItem Text="Help" Icon="@SgHeroicons.Outline.QuestionMarkCircle" OnClick="@(() => Log("Help"))" />
                    </ChildContent>
                </SgDropdown>
            </SgCard>
        </div>
    </div>

    <!-- ═══════════════════════════════════════════════════════════════════ -->
    <!-- 4. Placements                                                      -->
    <!-- ═══════════════════════════════════════════════════════════════════ -->
    <div class="demo-section">
        <div class="demo-section-header">
            <SgTypography Variant="SgTypographyVariant.Heading5" Weight="SgTypographyWeight.Semibold">
                Placements
            </SgTypography>
            <SgTypography Size="SgTypographySize.Sm" Color="var(--sg-text-muted)">
                Control menu position via <SgTypography As="SgTypographyElement.Code" Size="SgTypographySize.Sm">Placement</SgTypography>.
            </SgTypography>
        </div>

        <SgCard Title="Placement Variants" Shadow="SgShadow.Sm" Size="SgSize.Sm" NoPadding="true">
            <div style="padding:32px;display:flex;flex-wrap:wrap;gap:24px;align-items:flex-start;justify-content:center;min-height:280px;">
                <SgStack Vertical Space="SgSize.Md" Align="SgAlignItems.Center">
                    <SgTypography Size="SgTypographySize.Xs" Weight="SgTypographyWeight.Bold" Color="var(--sg-text-muted)">BottomStart</SgTypography>
                    <SgDropdown Text="BottomStart" Placement="SgPlacement.BottomStart">
                        <SgDropdownItem Text="Item 1" OnClick="@(() => Log("Placement"))" />
                        <SgDropdownItem Text="Item 2" OnClick="@(() => Log("Placement"))" />
                    </SgDropdown>
                </SgStack>
                <SgStack Vertical Space="SgSize.Md" Align="SgAlignItems.Center">
                    <SgTypography Size="SgTypographySize.Xs" Weight="SgTypographyWeight.Bold" Color="var(--sg-text-muted)">BottomEnd</SgTypography>
                    <SgDropdown Text="BottomEnd" Placement="SgPlacement.BottomEnd">
                        <SgDropdownItem Text="Item 1" OnClick="@(() => Log("Placement"))" />
                        <SgDropdownItem Text="Item 2" OnClick="@(() => Log("Placement"))" />
                    </SgDropdown>
                </SgStack>
                <SgStack Vertical Space="SgSize.Md" Align="SgAlignItems.Center">
                    <SgTypography Size="SgTypographySize.Xs" Weight="SgTypographyWeight.Bold" Color="var(--sg-text-muted)">TopStart</SgTypography>
                    <SgDropdown Text="TopStart" Placement="SgPlacement.TopStart">
                        <SgDropdownItem Text="Item 1" OnClick="@(() => Log("Placement"))" />
                        <SgDropdownItem Text="Item 2" OnClick="@(() => Log("Placement"))" />
                    </SgDropdown>
                </SgStack>
                <SgStack Vertical Space="SgSize.Md" Align="SgAlignItems.Center">
                    <SgTypography Size="SgTypographySize.Xs" Weight="SgTypographyWeight.Bold" Color="var(--sg-text-muted)">TopEnd</SgTypography>
                    <SgDropdown Text="TopEnd" Placement="SgPlacement.TopEnd">
                        <SgDropdownItem Text="Item 1" OnClick="@(() => Log("Placement"))" />
                        <SgDropdownItem Text="Item 2" OnClick="@(() => Log("Placement"))" />
                    </SgDropdown>
                </SgStack>
            </div>
        </SgCard>
    </div>

    <!-- ═══════════════════════════════════════════════════════════════════ -->
    <!-- 5. Trigger Modes                                                   -->
    <!-- ═══════════════════════════════════════════════════════════════════ -->
    <div class="demo-section">
        <div class="demo-section-header">
            <SgTypography Variant="SgTypographyVariant.Heading5" Weight="SgTypographyWeight.Semibold">
                Trigger Modes
            </SgTypography>
            <SgTypography Size="SgTypographySize.Sm" Color="var(--sg-text-muted)">
                <SgTypography As="SgTypographyElement.Code" Size="SgTypographySize.Sm">Click</SgTypography> (default), <SgTypography As="SgTypographyElement.Code" Size="SgTypographySize.Sm">Hover</SgTypography>, or <SgTypography As="SgTypographyElement.Code" Size="SgTypographySize.Sm">ContextMenu</SgTypography>.
            </SgTypography>
        </div>

        <SgRow Space="SgSize.Lg">
            <SgCol Md="4">
                <SgCard Title="Click (Default)" Shadow="SgShadow.Sm" Size="SgSize.Sm">
                    <SgTypography Size="SgTypographySize.Xs" Color="var(--sg-text-muted)" Style="display:block;margin-bottom:12px;">
                        Click to open, click item or outside to close.
                    </SgTypography>
                    <SgDropdown Text="Click me" ShowCaret="true">
                        <SgDropdownItem Text="Option A" OnClick="@(() => Log("Click A"))" />
                        <SgDropdownItem Text="Option B" OnClick="@(() => Log("Click B"))" />
                    </SgDropdown>
                </SgCard>
            </SgCol>
            <SgCol Md="4">
                <SgCard Title="Hover" Shadow="SgShadow.Sm" Size="SgSize.Sm">
                    <SgTypography Size="SgTypographySize.Xs" Color="var(--sg-text-muted)" Style="display:block;margin-bottom:12px;">
                        Hover to open with configurable delay.
                    </SgTypography>
                    <SgDropdown Text="Hover me" Trigger="SgDropdownTrigger.Hover" ShowCaret="true" OpenDelay="150" CloseDelay="200">
                        <SgDropdownItem Text="Option A" OnClick="@(() => Log("Hover A"))" />
                        <SgDropdownItem Text="Option B" OnClick="@(() => Log("Hover B"))" />
                    </SgDropdown>
                </SgCard>
            </SgCol>
            <SgCol Md="4">
                <SgCard Title="Context Menu" Shadow="SgShadow.Sm" Size="SgSize.Sm">
                    <SgTypography Size="SgTypographySize.Xs" Color="var(--sg-text-muted)" Style="display:block;margin-bottom:12px;">
                        Right-click to open.
                    </SgTypography>
                    <SgDropdown Text="Right-click me" Trigger="SgDropdownTrigger.ContextMenu" ShowCaret="true">
                        <SgDropdownItem Text="Option A" OnClick="@(() => Log("Context A"))" />
                        <SgDropdownItem Text="Option B" OnClick="@(() => Log("Context B"))" />
                    </SgDropdown>
                </SgCard>
            </SgCol>
        </SgRow>
    </div>

    <!-- ═══════════════════════════════════════════════════════════════════ -->
    <!-- 6. Searchable                                                      -->
    <!-- ═══════════════════════════════════════════════════════════════════ -->
    <div class="demo-section">
        <div class="demo-section-header">
            <SgTypography Variant="SgTypographyVariant.Heading5" Weight="SgTypographyWeight.Semibold">
                Searchable
            </SgTypography>
            <SgTypography Size="SgTypographySize.Sm" Color="var(--sg-text-muted)">
                Filter items by typing when <SgTypography As="SgTypographyElement.Code" Size="SgTypographySize.Sm">Searchable="true"</SgTypography>.
            </SgTypography>
        </div>

        <div class="demo-two-col">
            <SgCard Title="Searchable Dropdown" Shadow="SgShadow.Sm" Size="SgSize.Sm">
                <SgDropdown Text="Select country" Searchable="true" ShowCaret="true" MinWidth="220">
                    <SgDropdownItem Text="United States" Icon="@SgHeroicons.Outline.GlobeAmericas" Subtext="North America" OnClick="@(() => Log("US"))" />
                    <SgDropdownItem Text="United Kingdom" Icon="@SgHeroicons.Outline.GlobeEuropeAfrica" Subtext="Europe" OnClick="@(() => Log("UK"))" />
                    <SgDropdownItem Text="Germany" Icon="@SgHeroicons.Outline.GlobeEuropeAfrica" Subtext="Europe" OnClick="@(() => Log("Germany"))" />
                    <SgDropdownItem Text="France" Icon="@SgHeroicons.Outline.GlobeEuropeAfrica" Subtext="Europe" OnClick="@(() => Log("France"))" />
                    <SgDropdownItem Text="Japan" Icon="@SgHeroicons.Outline.GlobeAsiaAustralia" Subtext="Asia" OnClick="@(() => Log("Japan"))" />
                    <SgDropdownItem Text="Brazil" Icon="@SgHeroicons.Outline.GlobeAmericas" Subtext="South America" OnClick="@(() => Log("Brazil"))" />
                    <SgDropdownItem Text="Australia" Icon="@SgHeroicons.Outline.GlobeAsiaAustralia" Subtext="Oceania" OnClick="@(() => Log("Australia"))" />
                    <SgDropdownItem Text="Canada" Icon="@SgHeroicons.Outline.GlobeAmericas" Subtext="North America" OnClick="@(() => Log("Canada"))" />
                </SgDropdown>
            </SgCard>

            <SgCard Title="New Properties" Shadow="SgShadow.Sm" Size="SgSize.Sm">
                <PropertyTable Items="@(new List<PropertyPanelItem>
                {
                    new() { Label = "Searchable", Value = "bool", BadgeText = "Show search", BadgeVariant = SgBadgeVariant.Success },
                    new() { Label = "SearchPlaceholder", Value = "string?", BadgeText = "Input hint" },
                    new() { Label = "Loading", Value = "bool", BadgeText = "Loading state", BadgeVariant = SgBadgeVariant.Info },
                    new() { Label = "MatchWidth", Value = "bool", BadgeText = "Match trigger width" },
                    new() { Label = "Gap", Value = "string?", BadgeText = "Custom gap" },
                    new() { Label = "MenuMaxWidth", Value = "int?", BadgeText = "Menu max width" },
                    new() { Label = "Animation", Value = "bool", BadgeText = "Animation", BadgeVariant = SgBadgeVariant.Success },
                })" />
            </SgCard>
        </div>
    </div>

    <!-- ═══════════════════════════════════════════════════════════════════ -->
    <!-- 7. Items with Subtext & Shortcuts                                  -->
    <!-- ═══════════════════════════════════════════════════════════════════ -->
    <div class="demo-section">
        <div class="demo-section-header">
            <SgTypography Variant="SgTypographyVariant.Heading5" Weight="SgTypographyWeight.Semibold">
                Subtext, Shortcuts & Selection
            </SgTypography>
            <SgTypography Size="SgTypographySize.Sm" Color="var(--sg-text-muted)">
                Enhanced items with <SgTypography As="SgTypographyElement.Code" Size="SgTypographySize.Sm">Subtext</SgTypography>, <SgTypography As="SgTypographyElement.Code" Size="SgTypographySize.Sm">Shortcut</SgTypography>, and <SgTypography As="SgTypographyElement.Code" Size="SgTypographySize.Sm">Selected</SgTypography> state.
            </SgTypography>
        </div>

        <div class="demo-two-col">
            <SgCard Title="Rich Items" Shadow="SgShadow.Sm" Size="SgSize.Sm">
                <SgStack Vertical Space="SgSize.Lg">
                    <div>
                        <SgTypography Size="SgTypographySize.Xs" Weight="SgTypographyWeight.Bold" Transform="SgTypographyTransform.Uppercase" Tracking="SgTypographyTracking.Wide" Color="var(--sg-text-muted)" Style="display:block;margin-bottom:10px;border-bottom:1px solid var(--sg-border);padding-bottom:4px;">
                            With Subtext
                        </SgTypography>
                        <SgDropdown Text="Select view" Variant="SgButtonVariant.Outlined" ShowCaret="true" MinWidth="200">
                            <SgDropdownItem Text="Table" Icon="@SgHeroicons.Outline.TableCells" Subtext="Grid layout with columns" OnClick="@(() => Log("Table view"))" />
                            <SgDropdownItem Text="Cards" Icon="@SgHeroicons.Outline.Squares2x2" Subtext="Card-based layout" OnClick="@(() => Log("Cards view"))" />
                            <SgDropdownItem Text="Timeline" Icon="@SgHeroicons.Outline.ListBullet" Subtext="Chronological feed" OnClick="@(() => Log("Timeline view"))" />
                        </SgDropdown>
                    </div>
                    <div>
                        <SgTypography Size="SgTypographySize.Xs" Weight="SgTypographyWeight.Bold" Transform="SgTypographyTransform.Uppercase" Tracking="SgTypographyTracking.Wide" Color="var(--sg-text-muted)" Style="display:block;margin-bottom:10px;border-bottom:1px solid var(--sg-border);padding-bottom:4px;">
                            With Shortcuts
                        </SgTypography>
                        <SgDropdown Text="Edit" Variant="SgButtonVariant.Default" ShowCaret="true">
                            <SgDropdownItem Text="Undo" Shortcut="⌘Z" OnClick="@(() => Log("Undo"))" />
                            <SgDropdownItem Text="Redo" Shortcut="⌘⇧Z" OnClick="@(() => Log("Redo"))" />
                            <SgDropdownItem Divider="true" />
                            <SgDropdownItem Text="Cut" Shortcut="⌘X" OnClick="@(() => Log("Cut"))" />
                            <SgDropdownItem Text="Copy" Shortcut="⌘C" OnClick="@(() => Log("Copy"))" />
                            <SgDropdownItem Text="Paste" Shortcut="⌘V" OnClick="@(() => Log("Paste"))" />
                        </SgDropdown>
                    </div>
                </SgStack>
            </SgCard>

            <SgCard Title="Selected State" Shadow="SgShadow.Sm" Size="SgSize.Sm">
                <SgTypography Size="SgTypographySize.Xs" Color="var(--sg-text-muted)" Style="display:block;margin-bottom:12px;">
                    Use <SgTypography As="SgTypographyElement.Code" Size="SgTypographySize.Xs">Selected="true"</SgTypography> to show a checkmark.
                </SgTypography>
                <SgDropdown Text="Sort by" Variant="SgButtonVariant.Outlined" ShowCaret="true" MinWidth="180">
                    <SgDropdownItem Text="Name" Selected="@(_sortBy == "name")" OnClick="@(() => { _sortBy = "name"; Log("Sort: name"); })" />
                    <SgDropdownItem Text="Date" Selected="@(_sortBy == "date")" OnClick="@(() => { _sortBy = "date"; Log("Sort: date"); })" />
                    <SgDropdownItem Text="Size" Selected="@(_sortBy == "size")" OnClick="@(() => { _sortBy = "size"; Log("Sort: size"); })" />
                    <SgDropdownItem Text="Type" Selected="@(_sortBy == "type")" OnClick="@(() => { _sortBy = "type"; Log("Sort: type"); })" />
                </SgDropdown>
                <SgTypography Size="SgTypographySize.Xs" Color="var(--sg-text-muted)" Style="display:block;margin-top:12px;">
                    Current: <SgTypography As="SgTypographyElement.Code" Size="SgTypographySize.Xs" Weight="SgTypographyWeight.Semibold">@_sortBy</SgTypography>
                </SgTypography>
            </SgCard>
        </div>
    </div>

    <!-- ═══════════════════════════════════════════════════════════════════ -->
    <!-- 8. Loading State                                                   -->
    <!-- ═══════════════════════════════════════════════════════════════════ -->
    <div class="demo-section">
        <div class="demo-section-header">
            <SgTypography Variant="SgTypographyVariant.Heading5" Weight="SgTypographyWeight.Semibold">
                Loading State
            </SgTypography>
            <SgTypography Size="SgTypographySize.Sm" Color="var(--sg-text-muted)">
                Show a loading spinner via <SgTypography As="SgTypographyElement.Code" Size="SgTypographySize.Sm">Loading="true"</SgTypography>.
            </SgTypography>
        </div>

        <div class="demo-two-col">
            <SgCard Title="Loading Example" Shadow="SgShadow.Sm" Size="SgSize.Sm">
                <SgDropdown Text="@(_isLoading ? "Loading..." : "Load data")" Loading="@_isLoading" ShowCaret="true" Variant="@(_isLoading ? SgButtonVariant.Default : SgButtonVariant.Primary)">
                    @if (!_isLoading)
                    {
                        <SgDropdownItem Text="Users" Icon="@SgHeroicons.Outline.Users" OnClick="@(() => Log("Users"))" />
                        <SgDropdownItem Text="Posts" Icon="@SgHeroicons.Outline.DocumentText" OnClick="@(() => Log("Posts"))" />
                        <SgDropdownItem Text="Comments" Icon="@SgHeroicons.Outline.ChatBubbleLeft" OnClick="@(() => Log("Comments"))" />
                    }
                </SgDropdown>
                <SgButton Text="@(_isLoading ? "Stop loading" : "Toggle loading")" Variant="SgButtonVariant.Ghost" Size="SgSize.Sm" Style="margin-top:12px;" OnClick="@(() => _isLoading = !_isLoading)" />
            </SgCard>

            <SgCard Title="Loading Properties" Shadow="SgShadow.Sm" Size="SgSize.Sm">
                <PropertyTable Items="_loadingProperties" />
            </SgCard>
        </div>
    </div>

    <!-- ═══════════════════════════════════════════════════════════════════ -->
    <!-- 9. Colors & Badges                                                -->
    <!-- ═══════════════════════════════════════════════════════════════════ -->
    <div class="demo-section">
        <div class="demo-section-header">
            <SgTypography Variant="SgTypographyVariant.Heading5" Weight="SgTypographyWeight.Semibold">
                Semantic Colors & Badges
            </SgTypography>
            <SgTypography Size="SgTypographySize.Sm" Color="var(--sg-text-muted)">
                Color-coded items via <SgTypography As="SgTypographyElement.Code" Size="SgTypographySize.Sm">Color</SgTypography> param and inline badges via <SgTypography As="SgTypographyElement.Code" Size="SgTypographySize.Sm">BadgeText</SgTypography>.
            </SgTypography>
        </div>

        <div class="demo-two-col">
            <SgCard Title="Color Variants" Shadow="SgShadow.Sm" Size="SgSize.Sm">
                <SgDropdown Text="Status" Variant="SgButtonVariant.Outlined" ShowCaret="true" MinWidth="200">
                    <SgDropdownItem Text="Active" Color="SuperUI.Enums.SgColor.Success" Icon="@SgHeroicons.Outline.CheckCircle" OnClick="@(() => Log("Active"))" />
                    <SgDropdownItem Text="Pending" Color="SuperUI.Enums.SgColor.Warning" Icon="@SgHeroicons.Outline.Clock" OnClick="@(() => Log("Pending"))" />
                    <SgDropdownItem Text="Failed" Color="SuperUI.Enums.SgColor.Danger" Icon="@SgHeroicons.Outline.XCircle" OnClick="@(() => Log("Failed"))" />
                    <SgDropdownItem Text="Info" Color="SuperUI.Enums.SgColor.Info" Icon="@SgHeroicons.Outline.InformationCircle" OnClick="@(() => Log("Info"))" />
                </SgDropdown>
            </SgCard>
            <SgCard Title="With Badges" Shadow="SgShadow.Sm" Size="SgSize.Sm">
                <SgDropdown Text="Notifications" Variant="SgButtonVariant.Primary" ShowCaret="true" MinWidth="200">
                    <SgDropdownItem Text="Comments" BadgeText="12" BadgeVariant="SgBadgeVariant.Success" Icon="@SgHeroicons.Outline.ChatBubbleLeft" OnClick="@(() => Log("Comments"))" />
                    <SgDropdownItem Text="Likes" BadgeText="5" BadgeVariant="SgBadgeVariant.Warn" Icon="@SgHeroicons.Outline.Heart" OnClick="@(() => Log("Likes"))" />
                    <SgDropdownItem Text="Mentions" BadgeText="3" BadgeVariant="SgBadgeVariant.Info" Icon="@SgHeroicons.Outline.AtSymbol" OnClick="@(() => Log("Mentions"))" />
                    <SgDropdownItem Text="Errors" BadgeText="1" BadgeVariant="SgBadgeVariant.Danger" Icon="@SgHeroicons.Outline.ExclamationTriangle" OnClick="@(() => Log("Errors"))" />
                </SgDropdown>
            </SgCard>
        </div>
    </div>

    <!-- ═══════════════════════════════════════════════════════════════════ -->
    <!-- 10. Checkbox Items                                                -->
    <!-- ═══════════════════════════════════════════════════════════════════ -->
    <div class="demo-section">
        <div class="demo-section-header">
            <SgTypography Variant="SgTypographyVariant.Heading5" Weight="SgTypographyWeight.Semibold">
                Checkbox Items
            </SgTypography>
            <SgTypography Size="SgTypographySize.Sm" Color="var(--sg-text-muted)">
                Toggleable items with <SgTypography As="SgTypographyElement.Code" Size="SgTypographySize.Sm">Checkbox</SgTypography>, <SgTypography As="SgTypographyElement.Code" Size="SgTypographySize.Sm">Checked</SgTypography> and <SgTypography As="SgTypographyElement.Code" Size="SgTypographySize.Sm">CheckedChanged</SgTypography>.
            </SgTypography>
        </div>

        <div class="demo-two-col">
            <SgCard Title="Filter Panel" Shadow="SgShadow.Sm" Size="SgSize.Sm">
                <SgDropdown Text="Filters" Variant="SgButtonVariant.Outlined" ShowCaret="true" CloseOnSelect="false" MinWidth="200">
                    <SgDropdownItem Checkbox="true" Checked="@_filterUsers" CheckedChanged="@HandleFilterUsersChanged" Text="Users" />
                    <SgDropdownItem Checkbox="true" Checked="@_filterPosts" CheckedChanged="@HandleFilterPostsChanged" Text="Posts" />
                    <SgDropdownItem Checkbox="true" Checked="@_filterComments" CheckedChanged="@HandleFilterCommentsChanged" Text="Comments" />
                </SgDropdown>
                <SgTypography Size="SgTypographySize.Xs" Color="var(--sg-text-muted)" Style="display:block;margin-top:12px;">
                    Users: @_filterUsers, Posts: @_filterPosts, Comments: @_filterComments
                </SgTypography>
            </SgCard>
            <SgCard Title="Multi-Select" Shadow="SgShadow.Sm" Size="SgSize.Sm">
                <SgDropdown Text="Tags" Variant="SgButtonVariant.Default" ShowCaret="true" CloseOnSelect="false" MinWidth="180">
                    <SgDropdownItem Checkbox="true" Checked="@_tagUrgent" CheckedChanged="@HandleTagUrgentChanged" Text="Urgent" BadgeText="!" BadgeVariant="SgBadgeVariant.Danger" />
                    <SgDropdownItem Checkbox="true" Checked="@_tagBug" CheckedChanged="@HandleTagBugChanged" Text="Bug" />
                    <SgDropdownItem Checkbox="true" Checked="@_tagFeature" CheckedChanged="@HandleTagFeatureChanged" Text="Feature" />
                </SgDropdown>
            </SgCard>
        </div>
    </div>

    <!-- ═══════════════════════════════════════════════════════════════════ -->
    <!-- 11. Compact & Dense                                               -->
    <!-- ═══════════════════════════════════════════════════════════════════ -->
    <div class="demo-section">
        <div class="demo-section-header">
            <SgTypography Variant="SgTypographyVariant.Heading5" Weight="SgTypographyWeight.Semibold">
                Compact / Dense Mode
            </SgTypography>
            <SgTypography Size="SgTypographySize.Sm" Color="var(--sg-text-muted)">
                Compact menu via <SgTypography As="SgTypographyElement.Code" Size="SgTypographySize.Sm">Compact</SgTypography> and dense items via <SgTypography As="SgTypographyElement.Code" Size="SgTypographySize.Sm">Dense</SgTypography>.
            </SgTypography>
        </div>

        <SgRow Space="SgSize.Lg">
            <SgCol Md="4">
                <SgCard Title="Normal" Shadow="SgShadow.Sm" Size="SgSize.Sm">
                    <SgDropdown Text="Normal" Variant="SgButtonVariant.Default" ShowCaret="true">
                        <SgDropdownItem Text="Item 1" Icon="@SgHeroicons.Outline.Document" OnClick="@(() => Log("Normal 1"))" />
                        <SgDropdownItem Text="Item 2" Icon="@SgHeroicons.Outline.Folder" OnClick="@(() => Log("Normal 2"))" />
                        <SgDropdownItem Divider="true" />
                        <SgDropdownItem Text="Item 3" Icon="@SgHeroicons.Outline.Trash" OnClick="@(() => Log("Normal 3"))" />
                    </SgDropdown>
                </SgCard>
            </SgCol>
            <SgCol Md="4">
                <SgCard Title="Compact Menu" Shadow="SgShadow.Sm" Size="SgSize.Sm">
                    <SgDropdown Text="Compact" Compact="true" Variant="SgButtonVariant.Outlined" ShowCaret="true">
                        <SgDropdownItem Text="Item A" OnClick="@(() => Log("Compact A"))" />
                        <SgDropdownItem Text="Item B" OnClick="@(() => Log("Compact B"))" />
                        <SgDropdownItem Divider="true" />
                        <SgDropdownItem Text="Item C" OnClick="@(() => Log("Compact C"))" />
                    </SgDropdown>
                </SgCard>
            </SgCol>
            <SgCol Md="4">
                <SgCard Title="Dense Items" Shadow="SgShadow.Sm" Size="SgSize.Sm">
                    <SgDropdown Text="Dense" Compact="true" Variant="SgButtonVariant.Ghost" ShowCaret="true">
                        <SgDropdownItem Text="Action 1" Dense="true" Icon="@SgHeroicons.Outline.Pencil" OnClick="@(() => Log("Dense 1"))" />
                        <SgDropdownItem Text="Action 2" Dense="true" Icon="@SgHeroicons.Outline.DocumentDuplicate" OnClick="@(() => Log("Dense 2"))" />
                        <SgDropdownItem Divider="true" />
                        <SgDropdownItem Text="Action 3" Dense="true" Icon="@SgHeroicons.Outline.ArchiveBox" OnClick="@(() => Log("Dense 3"))" />
                    </SgDropdown>
                </SgCard>
            </SgCol>
        </SgRow>
    </div>

    <!-- ═══════════════════════════════════════════════════════════════════ -->
    <!-- 12. Async Click                                                   -->
    <!-- ═══════════════════════════════════════════════════════════════════ -->
    <div class="demo-section">
        <div class="demo-section-header">
            <SgTypography Variant="SgTypographyVariant.Heading5" Weight="SgTypographyWeight.Semibold">
                Async Click
            </SgTypography>
            <SgTypography Size="SgTypographySize.Sm" Color="var(--sg-text-muted)">
                Uses <SgTypography As="SgTypographyElement.Code" Size="SgTypographySize.Sm">OnAsyncClick</SgTypography> to show an inline spinner while the task runs.
            </SgTypography>
        </div>

        <div class="demo-two-col">
            <SgCard Title="Async Operations" Shadow="SgShadow.Sm" Size="SgSize.Sm">
                <SgDropdown Text="Save as" Variant="SgButtonVariant.Primary" ShowCaret="true" MinWidth="200">
                    <SgDropdownItem Text="Save PDF" Icon="@SgHeroicons.Outline.DocumentArrowDown" Subtext="Portable Document Format" OnAsyncClick="@SaveAsPdfAsync" />
                    <SgDropdownItem Text="Save DOCX" Icon="@SgHeroicons.Outline.DocumentText" Subtext="Word Document" OnAsyncClick="@SaveAsDocxAsync" />
                    <SgDropdownItem Text="Save CSV" Icon="@SgHeroicons.Outline.TableCells" Subtext="Comma-Separated Values" OnAsyncClick="@SaveAsCsvAsync" />
                </SgDropdown>
                <SgTypography Size="SgTypographySize.Xs" Color="var(--sg-text-muted)" Style="display:block;margin-top:12px;">
                    Click an item to simulate a 2-second async operation with a spinner.
                </SgTypography>
            </SgCard>
            <SgCard Title="Properties" Shadow="SgShadow.Sm" Size="SgSize.Sm">
                <PropertyTable Items="@(new List<PropertyPanelItem>
                {
                    new() { Label = "OnAsyncClick", Value = "Func<Task>?", BadgeText = "Async handler", BadgeVariant = SgBadgeVariant.Info },
                    new() { Label = "Title", Value = "string?", BadgeText = "Tooltip text" },
                    new() { Label = "Dense", Value = "bool", BadgeText = "Compact item style" },
                })" />
            </SgCard>
        </div>
    </div>

    <!-- ═══════════════════════════════════════════════════════════════════ -->
    <!-- 13. Submenu                                                       -->
    <!-- ═══════════════════════════════════════════════════════════════════ -->
    <div class="demo-section">
        <div class="demo-section-header">
            <SgTypography Variant="SgTypographyVariant.Heading5" Weight="SgTypographyWeight.Semibold">
                Nested Submenus
            </SgTypography>
            <SgTypography Size="SgTypographySize.Sm" Color="var(--sg-text-muted)">
                Use <SgTypography As="SgTypographyElement.Code" Size="SgTypographySize.Sm">SgDropdownSub</SgTypography> for nested menus with flyout.
            </SgTypography>
        </div>

        <div class="demo-two-col">
            <SgCard Title="Nested Menu Demo" Shadow="SgShadow.Sm" Size="SgSize.Sm">
                <SgDropdown Text="Edit" Variant="SgButtonVariant.Default" ShowCaret="true" MinWidth="180">
                    <SgDropdownItem Text="Undo" Shortcut="⌘Z" Icon="@SgHeroicons.Outline.ArrowUturnLeft" OnClick="@(() => Log("Undo"))" />
                    <SgDropdownItem Text="Redo" Shortcut="⌘⇧Z" Icon="@SgHeroicons.Outline.ArrowUturnRight" OnClick="@(() => Log("Redo"))" />
                    <SgDropdownItem Divider="true" />
                    <SgDropdownSub Text="Insert" Icon="@SgHeroicons.Outline.Plus">
                        <SgDropdownItem Text="Image" Icon="@SgHeroicons.Outline.Photo" OnClick="@(() => Log("Insert Image"))" />
                        <SgDropdownItem Text="Link" Icon="@SgHeroicons.Outline.Link" OnClick="@(() => Log("Insert Link"))" />
                        <SgDropdownItem Text="Table" Icon="@SgHeroicons.Outline.TableCells" OnClick="@(() => Log("Insert Table"))" />
                        <SgDropdownItem Divider="true" />
                        <SgDropdownSub Text="Special" Icon="@SgHeroicons.Outline.Star">
                            <SgDropdownItem Text="Date" OnClick="@(() => Log("Insert Date"))" />
                            <SgDropdownItem Text="Time" OnClick="@(() => Log("Insert Time"))" />
                        </SgDropdownSub>
                    </SgDropdownSub>
                    <SgDropdownItem Divider="true" />
                    <SgDropdownSub Text="Find & Replace" Icon="@SgHeroicons.Outline.MagnifyingGlass">
                        <SgDropdownItem Text="Find" Shortcut="⌘F" OnClick="@(() => Log("Find"))" />
                        <SgDropdownItem Text="Replace" Shortcut="⌘H" OnClick="@(() => Log("Replace"))" />
                    </SgDropdownSub>
                </SgDropdown>
            </SgCard>
            <SgCard Title="SgDropdownSub Properties" Shadow="SgShadow.Sm" Size="SgSize.Sm">
                <PropertyTable Items="@(new List<PropertyPanelItem>
                {
                    new() { Label = "Text", Value = "string", BadgeText = "Submenu label" },
                    new() { Label = "Icon", Value = "string?", BadgeText = "Leading icon", BadgeVariant = SgBadgeVariant.Info },
                    new() { Label = "Disabled", Value = "bool", BadgeText = "Disabled", BadgeVariant = SgBadgeVariant.Warn },
                    new() { Label = "ChildContent", Value = "RenderFragment", BadgeText = "Submenu items" },
                })" />
            </SgCard>
        </div>
    </div>

    <!-- ═══════════════════════════════════════════════════════════════════ -->
    <!-- 14. Transitions & Arrow                                           -->
    <!-- ═══════════════════════════════════════════════════════════════════ -->
    <div class="demo-section">
        <div class="demo-section-header">
            <SgTypography Variant="SgTypographyVariant.Heading5" Weight="SgTypographyWeight.Semibold">
                Transitions & Arrow Indicator
            </SgTypography>
            <SgTypography Size="SgTypographySize.Sm" Color="var(--sg-text-muted)">
                Choose animation type via <SgTypography As="SgTypographyElement.Code" Size="SgTypographySize.Sm">DropdownTransition</SgTypography> and show a pointer arrow via <SgTypography As="SgTypographyElement.Code" Size="SgTypographySize.Sm">ShowArrow</SgTypography>.
            </SgTypography>
        </div>

        <SgRow Space="SgSize.Lg">
            <SgCol Md="4">
                <SgCard Title="Scale (Default)" Shadow="SgShadow.Sm" Size="SgSize.Sm">
                    <SgDropdown Text="Scale" ShowCaret="true" DropdownTransition="SgDropdownTransition.Scale" ShowArrow="true">
                        <SgDropdownItem Text="Item 1" OnClick="@(() => Log("Scale 1"))" />
                        <SgDropdownItem Text="Item 2" OnClick="@(() => Log("Scale 2"))" />
                    </SgDropdown>
                </SgCard>
            </SgCol>
            <SgCol Md="4">
                <SgCard Title="Slide" Shadow="SgShadow.Sm" Size="SgSize.Sm">
                    <SgDropdown Text="Slide" ShowCaret="true" DropdownTransition="SgDropdownTransition.Slide" ShowArrow="true">
                        <SgDropdownItem Text="Item 1" OnClick="@(() => Log("Slide 1"))" />
                        <SgDropdownItem Text="Item 2" OnClick="@(() => Log("Slide 2"))" />
                    </SgDropdown>
                </SgCard>
            </SgCol>
            <SgCol Md="4">
                <SgCard Title="Fade" Shadow="SgShadow.Sm" Size="SgSize.Sm">
                    <SgDropdown Text="Fade" ShowCaret="true" DropdownTransition="SgDropdownTransition.Fade" ShowArrow="true">
                        <SgDropdownItem Text="Item 1" OnClick="@(() => Log("Fade 1"))" />
                        <SgDropdownItem Text="Item 2" OnClick="@(() => Log("Fade 2"))" />
                    </SgDropdown>
                </SgCard>
            </SgCol>
        </SgRow>
    </div>

    <!-- ═══════════════════════════════════════════════════════════════════ -->
    <!-- 15. Portal & Flip                                                 -->
    <!-- ═══════════════════════════════════════════════════════════════════ -->
    <div class="demo-section">
        <div class="demo-section-header">
            <SgTypography Variant="SgTypographyVariant.Heading5" Weight="SgTypographyWeight.Semibold">
                Portal & Flip
            </SgTypography>
            <SgTypography Size="SgTypographySize.Sm" Color="var(--sg-text-muted)">
                Portal mode (<SgTypography As="SgTypographyElement.Code" Size="SgTypographySize.Sm">UsePortal</SgTypography>) teleports the menu to body for overflow scenarios. Flip auto-adjusts when space is tight.
            </SgTypography>
        </div>

        <SgRow Space="SgSize.Lg">
            <SgCol Md="6">
                <SgCard Title="Portal Mode" Shadow="SgShadow.Sm" Size="SgSize.Sm">
                    <SgTypography Size="SgTypographySize.Xs" Color="var(--sg-text-muted)" Style="display:block;margin-bottom:12px;">
                        Menu is positioned via <code>position:fixed</code> — useful inside scroll containers.
                    </SgTypography>
                    <div style="overflow:hidden;border:1px solid var(--sg-border);border-radius:8px;padding:16px;position:relative;">
                        <SgDropdown Text="Portaled" UsePortal="true" ShowCaret="true" Variant="SgButtonVariant.Primary">
                            <SgDropdownItem Text="Option A" OnClick="@(() => Log("Portal A"))" />
                            <SgDropdownItem Text="Option B" OnClick="@(() => Log("Portal B"))" />
                            <SgDropdownItem Divider="true" />
                            <SgDropdownItem Text="Option C" OnClick="@(() => Log("Portal C"))" />
                        </SgDropdown>
                    </div>
                </SgCard>
            </SgCol>
            <SgCol Md="6">
                <SgCard Title="Flip" Shadow="SgShadow.Sm" Size="SgSize.Sm">
                    <SgTypography Size="SgTypographySize.Xs" Color="var(--sg-text-muted)" Style="display:block;margin-bottom:12px;">
                        Automatically flips X/Y axis when menu goes out of viewport.
                    </SgTypography>
                    <SgDropdown Text="Flip enabled" Flip="true" ShowCaret="true" Variant="SgButtonVariant.Outlined">
                        <SgDropdownItem Text="Item 1" OnClick="@(() => Log("Flip 1"))" />
                        <SgDropdownItem Text="Item 2" OnClick="@(() => Log("Flip 2"))" />
                        <SgDropdownItem Text="Item 3" OnClick="@(() => Log("Flip 3"))" />
                        <SgDropdownItem Text="Item 4" OnClick="@(() => Log("Flip 4"))" />
                        <SgDropdownItem Text="Item 5" OnClick="@(() => Log("Flip 5"))" />
                    </SgDropdown>
                </SgCard>
            </SgCol>
        </SgRow>
    </div>

    <!-- ═══════════════════════════════════════════════════════════════════ -->
    <!-- 16. Live Constructor                                              -->
    <!-- ═══════════════════════════════════════════════════════════════════ -->
    <div class="demo-section">
        <div class="demo-section-header">
            <SgTypography Variant="SgTypographyVariant.Heading5" Weight="SgTypographyWeight.Semibold">
                Live Dropdown Constructor
            </SgTypography>
            <SgTypography Size="SgTypographySize.Sm" Color="var(--sg-text-muted)">
                Configure parameters interactively and see the result in real time.
            </SgTypography>
        </div>

        <SgCard Shadow="SgShadow.Md" Radius="SgSize.Lg" NoPadding="true">
            <SgRow Space="SgSize.Md">

                <!-- ── Control Panel ── -->
                <SgCol Xs="12" Md="5" Style="padding:24px;border-right:1px solid var(--sg-border);">
                    <SgStack Vertical Space="SgSize.Lg">

                        <!-- Trigger text -->
                        <SgStack Vertical Space="SgSize.Sm">
                            <SgTypography Size="SgTypographySize.Xs" Weight="SgTypographyWeight.Bold" Transform="SgTypographyTransform.Uppercase" Tracking="SgTypographyTracking.Wide" Color="var(--sg-text-muted)">Trigger Text</SgTypography>
                            <SgTextBox @bind-Value="_constructorTriggerText" Placeholder="Button label" Block="true" />
                        </SgStack>

                        <!-- Variant -->
                        <SgStack Vertical Space="SgSize.Sm">
                            <SgTypography Size="SgTypographySize.Xs" Weight="SgTypographyWeight.Bold" Transform="SgTypographyTransform.Uppercase" Tracking="SgTypographyTracking.Wide" Color="var(--sg-text-muted)">Variant</SgTypography>
                            <SgSegmented TValue="SgButtonVariant"
                                         Value="_constructorVariant"
                                         ValueChanged="v => { _constructorVariant = v; UpdateConstructorCode(); }"
                                         Block="true"
                                         Size="sm"
                                         Options="_variantSegOptions" />
                        </SgStack>

                        <!-- Placement -->
                        <SgStack Vertical Space="SgSize.Sm">
                            <SgTypography Size="SgTypographySize.Xs" Weight="SgTypographyWeight.Bold" Transform="SgTypographyTransform.Uppercase" Tracking="SgTypographyTracking.Wide" Color="var(--sg-text-muted)">Placement</SgTypography>
                            <SgSelect TValue="SgPlacement"
                                      @bind-Value="_constructorPlacement"
                                      Items="_placementItems"
                                      LabelSelector="x => x.ToString()"
                                      Block="true" />
                        </SgStack>

                        <!-- Toggles -->
                        <SgRow Space="SgSize.Sm">
                            <SgCol Span="6">
                                <SgStack Vertical Space="SgSize.Sm">
                                    <SgTypography Size="SgTypographySize.Xs" Weight="SgTypographyWeight.Bold" Transform="SgTypographyTransform.Uppercase" Tracking="SgTypographyTracking.Wide" Color="var(--sg-text-muted)">ShowCaret</SgTypography>
                                    <SgSegmented TValue="bool"
                                                 Value="_constructorShowCaret"
                                                 ValueChanged="v => { _constructorShowCaret = v; UpdateConstructorCode(); }"
                                                 Block="true"
                                                 Size="sm"
                                                 Options="_boolSegOptions" />
                                </SgStack>
                            </SgCol>
                            <SgCol Span="6">
                                <SgStack Vertical Space="SgSize.Sm">
                                    <SgTypography Size="SgTypographySize.Xs" Weight="SgTypographyWeight.Bold" Transform="SgTypographyTransform.Uppercase" Tracking="SgTypographyTracking.Wide" Color="var(--sg-text-muted)">Searchable</SgTypography>
                                    <SgSegmented TValue="bool"
                                                 Value="_constructorSearchable"
                                                 ValueChanged="v => { _constructorSearchable = v; UpdateConstructorCode(); }"
                                                 Block="true"
                                                 Size="sm"
                                                 Options="_boolSegOptions" />
                                </SgStack>
                            </SgCol>
                        </SgRow>

                        <!-- Summary -->
                        <div class="ctrl-summary">
                            <SgStack Vertical Space="SgSize.Sm">
                                <SgStack Horizontal Space="SgSize.Sm" Align="SgAlignItems.Center" Justify="SgJustifyContent.SpaceBetween">
                                    <SgTypography Size="SgTypographySize.Sm" Color="var(--sg-text-muted)">Variant</SgTypography>
                                    <SgBadge Variant="SgBadgeVariant.Info" Text="@_constructorVariant.ToString()" Standalone="true" />
                                </SgStack>
                                <SgStack Horizontal Space="SgSize.Sm" Align="SgAlignItems.Center" Justify="SgJustifyContent.SpaceBetween">
                                    <SgTypography Size="SgTypographySize.Sm" Color="var(--sg-text-muted)">Placement</SgTypography>
                                    <SgBadge Variant="SgBadgeVariant.Info" Text="@_constructorPlacement.ToString()" Standalone="true" />
                                </SgStack>
                                <SgStack Horizontal Space="SgSize.Sm" Align="SgAlignItems.Center" Justify="SgJustifyContent.SpaceBetween">
                                    <SgTypography Size="SgTypographySize.Sm" Color="var(--sg-text-muted)">Searchable</SgTypography>
                                    <SgBadge Variant="@(_constructorSearchable ? SgBadgeVariant.Success : SgBadgeVariant.Default)" Text="@(_constructorSearchable ? "On" : "Off")" Standalone="true" />
                                </SgStack>
                            </SgStack>
                        </div>

                    </SgStack>
                </SgCol>

                <!-- ── Preview + Code ── -->
                <SgCol Xs="12" Md="7" Style="padding:24px;">
                    <SgStack Vertical Space="SgSize.Lg">

                        <!-- Preview -->
                        <SgStack Vertical Space="SgSize.Sm">
                            <SgTypography Size="SgTypographySize.Xs" Weight="SgTypographyWeight.Bold" Transform="SgTypographyTransform.Uppercase" Tracking="SgTypographyTracking.Wide" Color="var(--sg-text-muted)">Result</SgTypography>
                            <div class="constructor-preview" id="constructor-preview">
                                <SgDropdown Text="@_constructorTriggerText"
                                            Variant="@_constructorVariant"
                                            Placement="@_constructorPlacement"
                                            ShowCaret="@_constructorShowCaret"
                                            Searchable="@_constructorSearchable">
                                    <SgDropdownItem Text="View" Icon="@SgHeroicons.Outline.Eye" OnClick="@(() => Log("Constructor: View"))" />
                                    <SgDropdownItem Text="Edit" Icon="@SgHeroicons.Outline.Pencil" OnClick="@(() => Log("Constructor: Edit"))" />
                                    <SgDropdownItem Text="Delete" Icon="@SgHeroicons.Outline.Trash" Danger="true" OnClick="@(() => Log("Constructor: Delete"))" />
                                </SgDropdown>
                            </div>
                        </SgStack>

                        <!-- Code with Copy -->
                        <SgStack Vertical Space="SgSize.Sm">
                            <div style="display:flex;align-items:center;justify-content:space-between;">
                                <SgTypography Size="SgTypographySize.Xs" Weight="SgTypographyWeight.Bold" Transform="SgTypographyTransform.Uppercase" Tracking="SgTypographyTracking.Wide" Color="var(--sg-text-muted)">Generated Code</SgTypography>
                                <button type="button" class="demo-copy-btn" title="Copy to clipboard" @onclick="CopyCodeToClipboard">
                                    <span id="copy-icon">@_copyIcon</span>
                                    <span id="copy-text" style="font-size:11px;font-weight:600;">@_copyLabel</span>
                                </button>
                            </div>
                            <div class="code-block" id="code-block">
                                @foreach (var line in _generatedCodeLines)
                                {
                                    <code>@line</code>
                                }
                            </div>
                        </SgStack>

                    </SgStack>
                </SgCol>

            </SgRow>
        </SgCard>
    </div>

</ChildContent>
</SgCard>

<style>
    .demo-section { margin-top: 48px; }
    .demo-section-header { margin-bottom: 20px; }
    .demo-two-col {
        display: grid;
        grid-template-columns: 1fr 1fr;
        gap: 24px;
    }
    @@media (max-width: 768px) {
        .demo-two-col { grid-template-columns: 1fr; }
    }
    .demo-log {
        background: var(--sg-surface-alt);
        padding: 8px 10px;
        border-radius: var(--sg-radius);
        font-size: 11px;
        min-height: 48px;
        max-height: 160px;
        overflow-y: auto;
        font-family: 'JetBrains Mono', 'Fira Code', monospace;
    }
    .demo-log-entry {
        padding: 2px 0;
        color: var(--sg-fg);
        border-bottom: 1px solid var(--sg-divider);
    }
    .demo-log-entry:last-child { border-bottom: none; }
    .demo-log-empty { color: var(--sg-fg-muted); opacity: 0.6; }
    .demo-inline-btn {
        background: none; border: none; color: var(--sg-fg-muted);
        cursor: pointer; font-size: 12px; padding: 0 4px; margin-left: 8px;
        border-radius: 4px; transition: background 0.15s, color 0.15s;
    }
    .demo-inline-btn:hover { background: var(--sg-bg-subtle); color: var(--sg-fg); }
    .constructor-preview {
        background: var(--sg-surface-alt);
        border: 1px solid var(--sg-border);
        border-radius: 8px;
        padding: 24px;
        min-height: 80px;
        display: flex;
        align-items: center;
        transition: all 0.3s ease;
    }
    .ctrl-summary {
        background: var(--sg-surface-alt);
        border: 1px solid var(--sg-border);
        border-radius: 8px;
        padding: 12px 14px;
    }
    .code-block {
        background: #0f172a;
        color: #7dd3fc;
        padding: 14px 16px;
        border-radius: 8px;
        font-family: 'JetBrains Mono', 'Fira Code', monospace;
        font-size: 12px;
        line-height: 1.9;
        overflow-x: auto;
    }
    .code-block code {
        background: none; padding: 0; color: inherit;
        font-size: inherit; display: block; white-space: pre;
    }
    .demo-copy-btn {
        display: inline-flex; align-items: center; gap: 4px;
        background: var(--sg-surface-alt);
        border: 1px solid var(--sg-border);
        border-radius: 6px;
        padding: 4px 10px;
        cursor: pointer;
        color: var(--sg-text-muted);
        transition: all 0.2s ease;
        font-family: inherit;
    }
    .demo-copy-btn:hover {
        color: var(--sg-primary);
        border-color: var(--sg-primary);
        background: color-mix(in srgb, var(--sg-primary) 8%, transparent);
    }
    .demo-copy-btn:active { transform: scale(0.95); }
    .demo-copy-btn.copied { color: var(--sg-color-success); border-color: var(--sg-color-success); }
</style>

@code {
    // ── Main header ────────────────────────────────────────────────────
    private RenderFragment _mainHeader => __builder =>
    {
        <SgStack Horizontal Space="SgSize.Md" Align="SgAlignItems.Center">
            <div style="display:flex;align-items:center;justify-content:center;width:40px;height:40px;border-radius:10px;background:linear-gradient(135deg,var(--sg-primary),#8b5cf6);color:#fff;flex-shrink:0;">
                <svg viewBox="0 0 24 24" width="20" height="20" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                    <path d="M12 2a3 3 0 1 0 0 6 3 3 0 0 0 0-6z"/><path d="M12 22v-4"/><path d="M12 14v-2"/><path d="M12 10V8"/><path d="M4.93 4.93l2.83 2.83"/><path d="M19.07 4.93l-2.83 2.83"/><path d="M4.93 19.07l2.83-2.83"/><path d="M19.07 19.07l-2.83-2.83"/>
                </svg>
            </div>
            <SgStack Vertical Space="SgSize.Sm" Align="SgAlignItems.Start">
                <SgTypography Variant="SgTypographyVariant.Heading4" Weight="SgTypographyWeight.Bold">SgDropdown — Dropdown Menu</SgTypography>
                <SgTypography Size="SgTypographySize.Sm" Color="var(--sg-text-muted)">
                    Versatile dropdown with items, icons, search, keyboard navigation, subtext, multiple trigger modes, colors, badges, checkboxes, submenus, async click, compact mode, transitions, portal, and flip.
                </SgTypography>
            </SgStack>
        </SgStack>
    };

    // ── State ───────────────────────────────────────────────────────────
    private List<string> _logs = new();
    private string _sortBy = "name";
    private bool _isLoading;
    private bool _filterUsers = true;
    private bool _filterPosts;
    private bool _filterComments = true;
    private bool _tagUrgent;
    private bool _tagBug = true;
    private bool _tagFeature;

    private void Log(string msg)
    {
        _logs.Insert(0, $"[{DateTime.Now:HH:mm:ss}] {msg}");
        if (_logs.Count > 15) _logs.RemoveAt(15);
    }

    private void LogView() => Log("View clicked");
    private void LogEdit() => Log("Edit clicked");
    private void LogDelete() => Log("Delete clicked");
    private void ClearLogs() => _logs.Clear();

    // ── Checkbox handlers ──────────────────────────────────────────────
    private void HandleFilterUsersChanged(bool v) { _filterUsers = v; Log("Filter users: " + v); }
    private void HandleFilterPostsChanged(bool v) { _filterPosts = v; Log("Filter posts: " + v); }
    private void HandleFilterCommentsChanged(bool v) { _filterComments = v; Log("Filter comments: " + v); }
    private void HandleTagUrgentChanged(bool v) { _tagUrgent = v; Log("Tag urgent: " + v); }
    private void HandleTagBugChanged(bool v) { _tagBug = v; Log("Tag bug: " + v); }
    private void HandleTagFeatureChanged(bool v) { _tagFeature = v; Log("Tag feature: " + v); }

    // ── Async click handlers ───────────────────────────────────────────
    private async Task SaveAsPdfAsync()
    {
        Log("Saving PDF...");
        await Task.Delay(2000);
        Log("PDF saved.");
    }
    private async Task SaveAsDocxAsync()
    {
        Log("Saving DOCX...");
        await Task.Delay(2000);
        Log("DOCX saved.");
    }
    private async Task SaveAsCsvAsync()
    {
        Log("Saving CSV...");
        await Task.Delay(2000);
        Log("CSV saved.");
    }

    // ── Constructor state ───────────────────────────────────────────────
    private string _constructorTriggerText = "Actions";
    private SgButtonVariant _constructorVariant = SgButtonVariant.Default;
    private SgPlacement _constructorPlacement = SgPlacement.BottomStart;
    private bool _constructorShowCaret = true;
    private bool _constructorSearchable;

    private string[] _generatedCodeLines = Array.Empty<string>();
    private string _copyIcon = "📋";
    private string _copyLabel = "Copy";

    private readonly List<SgSegmentedOption<SgButtonVariant>> _variantSegOptions = new()
    {
        new() { Value = SgButtonVariant.Default, Label = "Default" },
        new() { Value = SgButtonVariant.Primary, Label = "Primary" },
        new() { Value = SgButtonVariant.Outlined, Label = "Outlined" },
        new() { Value = SgButtonVariant.Ghost, Label = "Ghost" },
    };

    private readonly List<SgSegmentedOption<bool>> _boolSegOptions = new()
    {
        new() { Value = false, Label = "Off" },
        new() { Value = true,  Label = "On" },
    };

    private readonly List<SgPlacement> _placementItems = new()
    {
        SgPlacement.BottomStart, SgPlacement.BottomEnd,
        SgPlacement.TopStart, SgPlacement.TopEnd
    };

    protected override void OnInitialized() => UpdateConstructorCode();

    private void UpdateConstructorCode()
    {
        var lines = new List<string>
        {
            $"<SgDropdown Text=\"{_constructorTriggerText}\""
        };
        if (_constructorVariant != SgButtonVariant.Default)
            lines.Add($"           Variant=\"SgButtonVariant.{_constructorVariant}\"");
        if (_constructorPlacement != SgPlacement.BottomStart)
            lines.Add($"           Placement=\"SgPlacement.{_constructorPlacement}\"");
        if (_constructorShowCaret)
            lines.Add("           ShowCaret=\"true\"");
        if (_constructorSearchable)
            lines.Add("           Searchable=\"true\"");
        lines.Add("           MinWidth=\"180\">");
        lines.Add("    <SgDropdownItem Text=\"View\" Icon=\"@SgHeroicons.Outline.Eye\" />");
        lines.Add("    <SgDropdownItem Text=\"Edit\" Icon=\"@SgHeroicons.Outline.Pencil\" />");
        lines.Add("    <SgDropdownItem Text=\"Delete\" Icon=\"@SgHeroicons.Outline.Trash\" Danger=\"true\" />");
        lines.Add("</SgDropdown>");
        _generatedCodeLines = lines.ToArray();
    }

    private async Task CopyCodeToClipboard()
    {
        var code = string.Join("\n", _generatedCodeLines);
        try
        {
            await JS.InvokeVoidAsync("navigator.clipboard.writeText", code);
            _copyIcon = "✓";
            _copyLabel = "Copied!";
            StateHasChanged();
            await Task.Delay(2000);
            _copyIcon = "📋";
            _copyLabel = "Copy";
            StateHasChanged();
        }
        catch
        {
            _copyIcon = "✗";
            _copyLabel = "Failed";
            StateHasChanged();
            await Task.Delay(1500);
            _copyIcon = "📋";
            _copyLabel = "Copy";
            StateHasChanged();
        }
    }

    // ── Property tables ─────────────────────────────────────────────────
    private List<PropertyPanelItem> _basicProperties = new()
    {
        new() { Label = "Text", Value = "string?", BadgeText = "Trigger label" },
        new() { Label = "Variant", Value = "SgButtonVariant", BadgeText = "Button style", BadgeVariant = SgBadgeVariant.Info },
        new() { Label = "Size", Value = "SgSize", BadgeText = "Button size", BadgeVariant = SgBadgeVariant.Info },
        new() { Label = "Disabled", Value = "bool", BadgeText = "Disabled", BadgeVariant = SgBadgeVariant.Warn },
        new() { Label = "TriggerContent", Value = "RenderFragment?", BadgeText = "Custom trigger" },
        new() { Label = "ChildContent", Value = "RenderFragment", BadgeText = "Menu items" },
        new() { Label = "HeaderContent", Value = "RenderFragment?", BadgeText = "Header area" },
        new() { Label = "FooterContent", Value = "RenderFragment?", BadgeText = "Footer area" },
        new() { Label = "ShowCaret", Value = "bool", BadgeText = "Caret arrow" },
        new() { Label = "CloseOnSelect", Value = "bool", BadgeText = "Auto-close", BadgeVariant = SgBadgeVariant.Success },
        new() { Label = "Placement", Value = "SgPlacement", BadgeText = "Menu position", BadgeVariant = SgBadgeVariant.Info },
        new() { Label = "Trigger", Value = "SgDropdownTrigger", BadgeText = "Open mode", BadgeVariant = SgBadgeVariant.Info },
        new() { Label = "MaxHeight", Value = "int?", BadgeText = "Max menu height" },
        new() { Label = "MinWidth", Value = "int?", BadgeText = "Min menu width" },
        new() { Label = "Loading", Value = "bool", BadgeText = "Loading state", BadgeVariant = SgBadgeVariant.Info },
        new() { Label = "Searchable", Value = "bool", BadgeText = "Search filter", BadgeVariant = SgBadgeVariant.Success },
        new() { Label = "SearchPlaceholder", Value = "string?", BadgeText = "Search hint" },
        new() { Label = "MatchWidth", Value = "bool", BadgeText = "Match trigger" },
        new() { Label = "Gap", Value = "string?", BadgeText = "Trigger gap" },
        new() { Label = "MenuMaxWidth", Value = "int?", BadgeText = "Max width" },
        new() { Label = "Animation", Value = "bool", BadgeText = "Animate", BadgeVariant = SgBadgeVariant.Success },
        new() { Label = "UsePortal", Value = "bool", BadgeText = "Fixed teleport", BadgeVariant = SgBadgeVariant.Success },
        new() { Label = "Flip", Value = "bool", BadgeText = "Auto-flip axis", BadgeVariant = SgBadgeVariant.Success },
        new() { Label = "ShowArrow", Value = "bool", BadgeText = "Arrow indicator" },
        new() { Label = "Compact", Value = "bool", BadgeText = "Compact menu", BadgeVariant = SgBadgeVariant.Info },
        new() { Label = "AutoFocusSearch", Value = "bool", BadgeText = "Auto-focus search" },
        new() { Label = "ShowSearchClear", Value = "bool", BadgeText = "Clear search btn" },
        new() { Label = "EmptyText", Value = "string?", BadgeText = "No results text" },
        new() { Label = "DropdownTransition", Value = "enum", BadgeText = "Anim style", BadgeVariant = SgBadgeVariant.Info },
        new() { Label = "CssClass / MenuCssClass", Value = "string?", BadgeText = "Custom CSS" },
    };

    private List<PropertyPanelItem> _itemProperties = new()
    {
        new() { Label = "Text", Value = "string", BadgeText = "Item label" },
        new() { Label = "Subtext", Value = "string?", BadgeText = "Description", BadgeVariant = SgBadgeVariant.Info },
        new() { Label = "Icon", Value = "string?", BadgeText = "SVG icon markup" },
        new() { Label = "IconColor", Value = "string?", BadgeText = "Icon color" },
        new() { Label = "IconEnd", Value = "string?", BadgeText = "Right-side icon", BadgeVariant = SgBadgeVariant.Info },
        new() { Label = "Shortcut", Value = "string?", BadgeText = "Keyboard hint" },
        new() { Label = "Disabled", Value = "bool", BadgeText = "Disabled", BadgeVariant = SgBadgeVariant.Warn },
        new() { Label = "Danger", Value = "bool", BadgeText = "Danger style", BadgeVariant = SgBadgeVariant.Danger },
        new() { Label = "Selected", Value = "bool", BadgeText = "Checkmark", BadgeVariant = SgBadgeVariant.Success },
        new() { Label = "Checkbox", Value = "bool", BadgeText = "Checkbox mode", BadgeVariant = SgBadgeVariant.Success },
        new() { Label = "Checked / CheckedChanged", Value = "bool", BadgeText = "Checkbox state" },
        new() { Label = "Color", Value = "SgColor?", BadgeText = "Semantic color", BadgeVariant = SgBadgeVariant.Info },
        new() { Label = "BadgeText", Value = "string?", BadgeText = "Inline badge" },
        new() { Label = "BadgeVariant", Value = "SgBadgeVariant", BadgeText = "Badge style", BadgeVariant = SgBadgeVariant.Info },
        new() { Label = "Title", Value = "string?", BadgeText = "Tooltip text" },
        new() { Label = "Dense", Value = "bool", BadgeText = "Compact padding" },
        new() { Label = "OnAsyncClick", Value = "Func<Task>?", BadgeText = "Async handler", BadgeVariant = SgBadgeVariant.Info },
        new() { Label = "Divider", Value = "bool", BadgeText = "Separator" },
        new() { Label = "Href", Value = "string?", BadgeText = "Link mode", BadgeVariant = SgBadgeVariant.Info },
        new() { Label = "OnClick", Value = "EventCallback", BadgeText = "Click event" },
        new() { Label = "ChildContent", Value = "RenderFragment?", BadgeText = "Custom content" },
    };

    private List<PropertyPanelItem> _loadingProperties = new()
    {
        new() { Label = "Loading", Value = "bool", BadgeText = "Loading state", BadgeVariant = SgBadgeVariant.Info },
    };
}
