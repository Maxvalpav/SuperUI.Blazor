@page "/progress-demo"
@using SuperUI.Components
@using SuperUI.Enums
@using Microsoft.JSInterop
@inject IJSRuntime JS
@implements IDisposable

<PageTitle>SgProgress — Progress Indicators — SuperUI</PageTitle>

<SgCard Variant="SgCardVariant.Default" Shadow="SgShadow.Md" Radius="SgSize.Lg"
        Status="SgCardStatus.Info" Hoverable="false"
        HeaderContent="@_mainHeader">
    <ChildContent>

    <!-- ═══════════════════════════════════════════════════════════════════ -->
    <!-- 1. Linear Progress                                                  -->
    <!-- ═══════════════════════════════════════════════════════════════════ -->
    <div class="demo-section">
        <div class="demo-section-header">
            <SgTypography Variant="SgTypographyVariant.Heading5" Weight="SgTypographyWeight.Semibold">
                SgProgress — Linear Progress
            </SgTypography>
            <SgTypography Size="SgTypographySize.Sm" Color="var(--sg-text-muted)">
                Universal progress indicator with support for multiple sizes, color variants, and special effects like gradients, glow, and animated stripes.
            </SgTypography>
        </div>

        <div class="demo-two-col">
            <SgCard Title="Layout Variants" Shadow="SgShadow.Sm" Size="SgSize.Sm">
                <SgStack Vertical Space="SgSize.Lg">
                    <div>
                        <SgTypography Size="SgTypographySize.Xs" Weight="SgTypographyWeight.Bold" Transform="SgTypographyTransform.Uppercase" Tracking="SgTypographyTracking.Wide" Color="var(--sg-text-muted)" Style="display:block;margin-bottom:10px;border-bottom:1px solid var(--sg-border);padding-bottom:4px;">
                            Sizes
                        </SgTypography>
                        <div style="display:flex;flex-direction:column;gap:12px;">
                            <SgProgress Value="40" Size="SgSize.Sm" Title="Small (Sm)" />
                            <SgProgress Value="55" Size="SgSize.Md" Title="Medium (Md)" />
                            <SgProgress Value="70" Size="SgSize.Lg" Title="Large (Lg)" />
                            <SgProgress Value="85" Size="SgSize.Xl" Title="Extra Large (Xl)" />
                        </div>
                    </div>
                    <SgDivider />
                    <div>
                        <SgTypography Size="SgTypographySize.Xs" Weight="SgTypographyWeight.Bold" Transform="SgTypographyTransform.Uppercase" Tracking="SgTypographyTracking.Wide" Color="var(--sg-text-muted)" Style="display:block;margin-bottom:10px;border-bottom:1px solid var(--sg-border);padding-bottom:4px;">
                            Color Variants
                        </SgTypography>
                        <div style="display:flex;flex-direction:column;gap:12px;">
                            <SgProgress Value="50" Variant="SgSpinnerVariant.Default" Title="Default" />
                            <SgProgress Value="60" Variant="SgSpinnerVariant.Info" Title="Info" />
                            <SgProgress Value="70" Variant="SgSpinnerVariant.Success" Title="Success" />
                            <SgProgress Value="80" Variant="SgSpinnerVariant.Warn" Title="Warning" />
                            <SgProgress Value="90" Variant="SgSpinnerVariant.Danger" Title="Danger" />
                        </div>
                    </div>
                    <SgDivider />
                    <div>
                        <SgTypography Size="SgTypographySize.Xs" Weight="SgTypographyWeight.Bold" Transform="SgTypographyTransform.Uppercase" Tracking="SgTypographyTracking.Wide" Color="var(--sg-text-muted)" Style="display:block;margin-bottom:10px;border-bottom:1px solid var(--sg-border);padding-bottom:4px;">
                            Special Effects
                        </SgTypography>
                        <div style="display:flex;flex-direction:column;gap:12px;">
                            <SgProgress Value="75" Gradient="true" Variant="SgSpinnerVariant.Info" Title="Gradient" Size="SgSize.Lg" />
                            <SgProgress Value="65" Glow="true" Variant="SgSpinnerVariant.Success" Title="Glow" Size="SgSize.Lg" />
                            <SgProgress Value="70" Striped="true" Animated="true" Variant="SgSpinnerVariant.Warn" Title="Striped + Animated" Size="SgSize.Lg" />
                        </div>
                    </div>
                </SgStack>
            </SgCard>

            <SgCard Title="Linear Properties" Shadow="SgShadow.Sm" Size="SgSize.Sm">
                <PropertyTable Items="_progressProperties" />
            </SgCard>
        </div>
    </div>

    <!-- ═══════════════════════════════════════════════════════════════════ -->
    <!-- 2. Circular Progress                                                -->
    <!-- ═══════════════════════════════════════════════════════════════════ -->
    <div class="demo-section">
        <div class="demo-section-header">
            <SgTypography Variant="SgTypographyVariant.Heading5" Weight="SgTypographyWeight.Semibold">
                SgProgress — Circular Progress
            </SgTypography>
            <SgTypography Size="SgTypographySize.Sm" Color="var(--sg-text-muted)">
                Compact circular progress ideal for dashboards, cards, and widget panels.
            </SgTypography>
        </div>

        <div class="demo-two-col">
            <SgCard Title="Variants & Sizes" Shadow="SgShadow.Sm" Size="SgSize.Sm">
                <SgStack Vertical Space="SgSize.Lg">
                    <div>
                        <SgTypography Size="SgTypographySize.Xs" Weight="SgTypographyWeight.Bold" Transform="SgTypographyTransform.Uppercase" Tracking="SgTypographyTracking.Wide" Color="var(--sg-text-muted)" Style="display:block;margin-bottom:10px;border-bottom:1px solid var(--sg-border);padding-bottom:4px;">
                            Sizes
                        </SgTypography>
                        <div style="display:flex;gap:24px;align-items:center;justify-content:center;padding:20px;">
                            <div style="text-align:center;">
                                <SgProgress Value="37" Circular="true" Size="SgSize.Sm" />
                                <div style="font-size:11px;color:var(--sg-text-muted);margin-top:8px;">Sm</div>
                            </div>
                            <div style="text-align:center;">
                                <SgProgress Value="52" Circular="true" Size="SgSize.Md" />
                                <div style="font-size:11px;color:var(--sg-text-muted);margin-top:8px;">Md</div>
                            </div>
                            <div style="text-align:center;">
                                <SgProgress Value="68" Circular="true" Size="SgSize.Lg" />
                                <div style="font-size:11px;color:var(--sg-text-muted);margin-top:8px;">Lg</div>
                            </div>
                            <div style="text-align:center;">
                                <SgProgress Value="83" Circular="true" Size="SgSize.Xl" />
                                <div style="font-size:11px;color:var(--sg-text-muted);margin-top:8px;">Xl</div>
                            </div>
                        </div>
                    </div>
                    <SgDivider />
                    <div>
                        <SgTypography Size="SgTypographySize.Xs" Weight="SgTypographyWeight.Bold" Transform="SgTypographyTransform.Uppercase" Tracking="SgTypographyTracking.Wide" Color="var(--sg-text-muted)" Style="display:block;margin-bottom:10px;border-bottom:1px solid var(--sg-border);padding-bottom:4px;">
                            Colors
                        </SgTypography>
                        <div style="display:flex;gap:20px;align-items:center;justify-content:center;padding:20px;flex-wrap:wrap;">
                            <SgProgress Value="30" Circular="true" Variant="SgSpinnerVariant.Default" />
                            <SgProgress Value="45" Circular="true" Variant="SgSpinnerVariant.Info" />
                            <SgProgress Value="60" Circular="true" Variant="SgSpinnerVariant.Success" />
                            <SgProgress Value="75" Circular="true" Variant="SgSpinnerVariant.Warn" />
                            <SgProgress Value="90" Circular="true" Variant="SgSpinnerVariant.Danger" />
                        </div>
                    </div>
                    <SgDivider />
                    <div>
                        <SgTypography Size="SgTypographySize.Xs" Weight="SgTypographyWeight.Bold" Transform="SgTypographyTransform.Uppercase" Tracking="SgTypographyTracking.Wide" Color="var(--sg-text-muted)" Style="display:block;margin-bottom:10px;border-bottom:1px solid var(--sg-border);padding-bottom:4px;">
                            With Effects
                        </SgTypography>
                        <div style="display:flex;gap:32px;align-items:center;justify-content:center;padding:20px;">
                            <SgProgress Value="65" Circular="true" Size="SgSize.Lg" Gradient="true" Variant="SgSpinnerVariant.Info" />
                            <SgProgress Value="82" Circular="true" Size="SgSize.Lg" Glow="true" Variant="SgSpinnerVariant.Success" />
                            <SgProgress Value="45" Circular="true" Size="SgSize.Lg" Gradient="true" Glow="true" Variant="SgSpinnerVariant.Danger" />
                        </div>
                    </div>
                </SgStack>
            </SgCard>

            <SgCard Title="Circular Properties" Shadow="SgShadow.Sm" Size="SgSize.Sm">
                <PropertyTable Items="_circularProperties" />
            </SgCard>
        </div>
    </div>

    <!-- ═══════════════════════════════════════════════════════════════════ -->
    <!-- 3. Segmented Progress                                               -->
    <!-- ═══════════════════════════════════════════════════════════════════ -->
    <div class="demo-section">
        <div class="demo-section-header">
            <SgTypography Variant="SgTypographyVariant.Heading5" Weight="SgTypographyWeight.Semibold">
                SgProgress — Segmented Progress
            </SgTypography>
            <SgTypography Size="SgTypographySize.Sm" Color="var(--sg-text-muted)">
                Split a progress bar into discrete segments to visualize milestones, steps, or phases.
            </SgTypography>
        </div>

        <div class="demo-two-col">
            <SgCard Title="Examples" Shadow="SgShadow.Sm" Size="SgSize.Sm">
                <SgStack Vertical Space="SgSize.Lg">
                    <SgProgress Value="60" Segmented="true" Segments="5" Variant="SgSpinnerVariant.Success" Title="5 Segments" />
                    <SgProgress Value="75" Segmented="true" Segments="10" Variant="SgSpinnerVariant.Info" Title="10 Segments" />
                    <SgProgress Value="85" Segmented="true" Segments="20" Variant="SgSpinnerVariant.Warn" Title="20 Segments" />
                </SgStack>
            </SgCard>

            <SgCard Title="Segmented Properties" Shadow="SgShadow.Sm" Size="SgSize.Sm">
                <PropertyTable Items="_segmentedProperties" />
            </SgCard>
        </div>
    </div>

    <!-- ═══════════════════════════════════════════════════════════════════ -->
    <!-- 4. Buffer / Streaming                                               -->
    <!-- ═══════════════════════════════════════════════════════════════════ -->
    <div class="demo-section">
        <div class="demo-section-header">
            <SgTypography Variant="SgTypographyVariant.Heading5" Weight="SgTypographyWeight.Semibold">
                SgProgress — Buffer & Streaming
            </SgTypography>
            <SgTypography Size="SgTypographySize.Sm" Color="var(--sg-text-muted)">
                Display buffer progress for video, audio, and streaming data with animated transitions.
            </SgTypography>
        </div>

        <div class="demo-two-col">
            <SgCard Title="Examples" Shadow="SgShadow.Sm" Size="SgSize.Sm">
                <SgStack Vertical Space="SgSize.Lg">
                    <SgProgress Value="@_videoProgress" BufferValue="@_videoBuffer" Title="Video Playback" Size="SgSize.Lg" Variant="SgSpinnerVariant.Info" />
                    <SgProgress Value="@_audioProgress" BufferValue="@_audioBuffer" Title="Audio Track" Size="SgSize.Md" Variant="SgSpinnerVariant.Success" Gradient="true" />
                    <SgDivider />
                    <SgProgress Value="@_downloadProgress" BufferValue="@_downloadBuffer" Title="File Download (125 MB)" Size="SgSize.Lg" Variant="SgSpinnerVariant.Warn" Glow="true" />
                    <div style="display:flex;gap:12px;">
                        <SgButton OnClick="StartDownload" Size="SgSize.Sm" Variant="SgButtonVariant.Primary" Text="Start Download" />
                        <SgButton OnClick="StopDownload" Size="SgSize.Sm" Variant="SgButtonVariant.Default" Text="Stop" />
                    </div>
                </SgStack>
            </SgCard>

            <SgCard Title="Buffer Properties" Shadow="SgShadow.Sm" Size="SgSize.Sm">
                <PropertyTable Items="_bufferProperties" />
            </SgCard>
        </div>
    </div>

    <!-- ═══════════════════════════════════════════════════════════════════ -->
    <!-- 5. Indeterminate                                                    -->
    <!-- ═══════════════════════════════════════════════════════════════════ -->
    <div class="demo-section">
        <div class="demo-section-header">
            <SgTypography Variant="SgTypographyVariant.Heading5" Weight="SgTypographyWeight.Semibold">
                SgProgress — Indeterminate Loading
            </SgTypography>
            <SgTypography Size="SgTypographySize.Sm" Color="var(--sg-text-muted)">
                Use when progress cannot be measured or the duration is unknown — linear and circular variants.
            </SgTypography>
        </div>

        <div class="demo-two-col">
            <SgCard Title="Examples" Shadow="SgShadow.Sm" Size="SgSize.Sm">
                <SgStack Vertical Space="SgSize.Lg">
                    <div>
                        <SgTypography Size="SgTypographySize.Xs" Weight="SgTypographyWeight.Bold" Transform="SgTypographyTransform.Uppercase" Tracking="SgTypographyTracking.Wide" Color="var(--sg-text-muted)" Style="display:block;margin-bottom:10px;border-bottom:1px solid var(--sg-border);padding-bottom:4px;">
                            Linear
                        </SgTypography>
                        <div style="display:flex;flex-direction:column;gap:12px;">
                            <SgProgress Indeterminate="true" Title="Loading data..." Size="SgSize.Md" Variant="SgSpinnerVariant.Info" />
                            <SgProgress Indeterminate="true" Title="Processing request..." Size="SgSize.Lg" Variant="SgSpinnerVariant.Success" Gradient="true" />
                            <SgProgress Indeterminate="true" Title="Syncing..." Size="SgSize.Md" Variant="SgSpinnerVariant.Warn" />
                        </div>
                    </div>
                    <SgDivider />
                    <div>
                        <SgTypography Size="SgTypographySize.Xs" Weight="SgTypographyWeight.Bold" Transform="SgTypographyTransform.Uppercase" Tracking="SgTypographyTracking.Wide" Color="var(--sg-text-muted)" Style="display:block;margin-bottom:10px;border-bottom:1px solid var(--sg-border);padding-bottom:4px;">
                            Circular
                        </SgTypography>
                        <div style="display:flex;gap:32px;align-items:center;justify-content:center;padding:20px;">
                            <div style="text-align:center;">
                                <SgProgress Indeterminate="true" Circular="true" Size="SgSize.Md" Variant="SgSpinnerVariant.Info" />
                                <div style="font-size:12px;color:var(--sg-text-muted);margin-top:8px;">Loading...</div>
                            </div>
                            <div style="text-align:center;">
                                <SgProgress Indeterminate="true" Circular="true" Size="SgSize.Lg" Variant="SgSpinnerVariant.Success" Gradient="true" />
                                <div style="font-size:12px;color:var(--sg-text-muted);margin-top:8px;">Processing...</div>
                            </div>
                            <div style="text-align:center;">
                                <SgProgress Indeterminate="true" Circular="true" Size="SgSize.Xl" Variant="SgSpinnerVariant.Danger" Glow="true" />
                                <div style="font-size:12px;color:var(--sg-text-muted);margin-top:8px;">Waiting...</div>
                            </div>
                        </div>
                    </div>
                </SgStack>
            </SgCard>

            <SgCard Title="Indeterminate Properties" Shadow="SgShadow.Sm" Size="SgSize.Sm">
                <PropertyTable Items="_indeterminateProperties" />
            </SgCard>
        </div>
    </div>

    <!-- ═══════════════════════════════════════════════════════════════════ -->
    <!-- 6. Vertical Progress                                                -->
    <!-- ═══════════════════════════════════════════════════════════════════ -->
    <div class="demo-section">
        <div class="demo-section-header">
            <SgTypography Variant="SgTypographyVariant.Heading5" Weight="SgTypographyWeight.Semibold">
                SgProgress — Vertical Orientation
            </SgTypography>
            <SgTypography Size="SgTypographySize.Sm" Color="var(--sg-text-muted)">
                Vertical progress bars for sidebars, dashboards, and tall layouts — supports all effects.
            </SgTypography>
        </div>

        <div class="demo-two-col">
            <SgCard Title="Examples" Shadow="SgShadow.Sm" Size="SgSize.Sm">
                <div style="display:flex;gap:32px;align-items:flex-end;justify-content:center;padding:40px 20px;">
                    <div style="text-align:center;">
                        <SgProgress Value="30" Vertical="true" Size="SgSize.Sm" Variant="SgSpinnerVariant.Default" />
                        <div style="font-size:11px;color:var(--sg-text-muted);margin-top:8px;">30%</div>
                    </div>
                    <div style="text-align:center;">
                        <SgProgress Value="50" Vertical="true" Size="SgSize.Md" Variant="SgSpinnerVariant.Info" Gradient="true" />
                        <div style="font-size:11px;color:var(--sg-text-muted);margin-top:8px;">50%</div>
                    </div>
                    <div style="text-align:center;">
                        <SgProgress Value="70" Vertical="true" Size="SgSize.Lg" Variant="SgSpinnerVariant.Success" Glow="true" />
                        <div style="font-size:11px;color:var(--sg-text-muted);margin-top:8px;">70%</div>
                    </div>
                    <div style="text-align:center;">
                        <SgProgress Value="90" Vertical="true" Size="SgSize.Xl" Variant="SgSpinnerVariant.Warn" Gradient="true" Glow="true" />
                        <div style="font-size:11px;color:var(--sg-text-muted);margin-top:8px;">90%</div>
                    </div>
                    <div style="text-align:center;">
                        <SgProgress Value="100" Vertical="true" Size="SgSize.Lg" Variant="SgSpinnerVariant.Danger" Striped="true" Animated="true" />
                        <div style="font-size:11px;color:var(--sg-text-muted);margin-top:8px;">100%</div>
                    </div>
                </div>
            </SgCard>

            <SgCard Title="Vertical Properties" Shadow="SgShadow.Sm" Size="SgSize.Sm">
                <PropertyTable Items="_verticalProperties" />
            </SgCard>
        </div>
    </div>

    <!-- ═══════════════════════════════════════════════════════════════════ -->
    <!-- 7. Custom Colors                                                    -->
    <!-- ═══════════════════════════════════════════════════════════════════ -->
    <div class="demo-section">
        <div class="demo-section-header">
            <SgTypography Variant="SgTypographyVariant.Heading5" Weight="SgTypographyWeight.Semibold">
                SgProgress — Custom Colors
            </SgTypography>
            <SgTypography Size="SgTypographySize.Sm" Color="var(--sg-text-muted)">
                Override the default color palette with any custom color using HEX, RGB, or CSS color values.
            </SgTypography>
        </div>

        <div class="demo-two-col">
            <SgCard Title="Examples" Shadow="SgShadow.Sm" Size="SgSize.Sm">
                <SgStack Vertical Space="SgSize.Lg">
                    <div>
                        <SgTypography Size="SgTypographySize.Xs" Weight="SgTypographyWeight.Bold" Transform="SgTypographyTransform.Uppercase" Tracking="SgTypographyTracking.Wide" Color="var(--sg-text-muted)" Style="display:block;margin-bottom:10px;border-bottom:1px solid var(--sg-border);padding-bottom:4px;">
                            Linear Custom Colors
                        </SgTypography>
                        <div style="display:flex;flex-direction:column;gap:12px;">
                            <SgProgress Value="65" Color="#9333ea" Title="Purple (#9333ea)" Size="SgSize.Lg" />
                            <SgProgress Value="75" Color="#ec4899" Title="Pink (#ec4899)" Size="SgSize.Lg" Glow="true" />
                            <SgProgress Value="85" Color="#14b8a6" Title="Teal (#14b8a6)" Size="SgSize.Lg" Striped="true" Animated="true" />
                        </div>
                    </div>
                    <SgDivider />
                    <div>
                        <SgTypography Size="SgTypographySize.Xs" Weight="SgTypographyWeight.Bold" Transform="SgTypographyTransform.Uppercase" Tracking="SgTypographyTracking.Wide" Color="var(--sg-text-muted)" Style="display:block;margin-bottom:10px;border-bottom:1px solid var(--sg-border);padding-bottom:4px;">
                            Circular Custom Colors
                        </SgTypography>
                        <div style="display:flex;gap:24px;align-items:center;justify-content:center;padding:20px;">
                            <SgProgress Value="60" Circular="true" Color="#9333ea" Size="SgSize.Lg" />
                            <SgProgress Value="75" Circular="true" Color="#ec4899" Size="SgSize.Lg" Glow="true" />
                            <SgProgress Value="90" Circular="true" Color="#14b8a6" Size="SgSize.Lg" Glow="true" />
                        </div>
                    </div>
                </SgStack>
            </SgCard>

            <SgCard Title="Custom Color Properties" Shadow="SgShadow.Sm" Size="SgSize.Sm">
                <PropertyTable Items="_colorProperties" />
            </SgCard>
        </div>
    </div>

    <!-- ═══════════════════════════════════════════════════════════════════ -->
    <!-- 8. Sticky Top (Page Load Indicator)                                 -->
    <!-- ═══════════════════════════════════════════════════════════════════ -->
    <div class="demo-section">
        <div class="demo-section-header">
            <SgTypography Variant="SgTypographyVariant.Heading5" Weight="SgTypographyWeight.Semibold">
                SgProgress — Sticky Top (Page Load Indicator)
            </SgTypography>
            <SgTypography Size="SgTypographySize.Sm" Color="var(--sg-text-muted)">
                A thin progress bar fixed to the top of the viewport — ideal for simulating page-level loading (like YouTube, GitHub).
            </SgTypography>
        </div>

        <div class="demo-two-col">
            <SgCard Title="Example" Shadow="SgShadow.Sm" Size="SgSize.Sm">
                <SgStack Vertical Space="SgSize.Lg">
                    <div style="padding:20px;text-align:center;">
                        <SgButton OnClick="SimulatePageLoad"
                                 Variant="SgButtonVariant.Primary"
                                 Size="SgSize.Lg">
                            <SgSpace>
                                @((MarkupString)SgHeroicons.Outline.ArrowPathRoundedSquare)
                                <span>Simulate Page Load</span>
                            </SgSpace>
                        </SgButton>
                    </div>
                    <SgDivider />
                    <div style="padding:12px;background:var(--sg-surface-alt);border-radius:6px;">
                        <SgStack Horizontal Space="SgSize.Sm" Align="SgAlignItems.Center">
                            @((MarkupString)SgHeroicons.Outline.LightBulb)
                            <SgTypography Size="SgTypographySize.Sm" Color="var(--sg-text-muted)">
                                Clicking the button displays a thin progress strip fixed at the very top of the page, mimicking the behavior of YouTube or GitHub page load indicators.
                            </SgTypography>
                        </SgStack>
                    </div>
                    <SgDivider />
                    <div>
                        <SgTypography Size="SgTypographySize.Xs" Weight="SgTypographyWeight.Bold" Transform="SgTypographyTransform.Uppercase" Tracking="SgTypographyTracking.Wide" Color="var(--sg-text-muted)">Current State</SgTypography>
                        <div style="padding:12px;background:var(--sg-surface-alt);border-radius:6px;font-family:monospace;font-size:12px;">
                            <div>Shown: <strong>@(_showStickyProgress ? "Yes" : "No")</strong></div>
                            @if (_showStickyProgress)
                            {
                                <div style="margin-top:4px;">Progress: <strong>@($"{_stickyProgress:F0}%")</strong></div>
                            }
                        </div>
                    </div>
                </SgStack>
            </SgCard>

            <SgCard Title="Sticky Top Properties" Shadow="SgShadow.Sm" Size="SgSize.Sm">
                <PropertyTable Items="_stickyProperties" />
                <div style="margin-top:16px;">
                    <div style="padding:12px;background:var(--sg-surface-alt);border-radius:6px;">
                        <SgStack Horizontal Space="SgSize.Sm" Align="SgAlignItems.Start">
                            @((MarkupString)SgHeroicons.Outline.InformationCircle)
                            <SgTypography Size="SgTypographySize.Sm" Color="var(--sg-text-muted)">
                                StickyTop uses <SgTypography As="SgTypographyElement.Code" Size="SgTypographySize.Sm">position: fixed</SgTypography>. Typically used with <SgTypography As="SgTypographyElement.Code" Size="SgTypographySize.Sm">Size="SgSize.Sm"</SgTypography> and <SgTypography As="SgTypographyElement.Code" Size="SgTypographySize.Sm">ShowLabel="false"</SgTypography> for a minimal, elegant indicator.
                            </SgTypography>
                        </SgStack>
                    </div>
                </div>
            </SgCard>
        </div>
    </div>

    @if (_showStickyProgress)
    {
        <SgProgress Value="@_stickyProgress"
                   StickyTop="true"
                   Size="SgSize.Sm"
                   Variant="SgSpinnerVariant.Info"
                   Gradient="true"
                   ShowLabel="false" />
    }

    <!-- ═══════════════════════════════════════════════════════════════════ -->
    <!-- 9. Live Constructor                                                 -->
    <!-- ═══════════════════════════════════════════════════════════════════ -->
    <div class="demo-section">
        <div class="demo-section-header">
            <SgTypography Variant="SgTypographyVariant.Heading5" Weight="SgTypographyWeight.Semibold">
                Live SgProgress Constructor
            </SgTypography>
            <SgTypography Size="SgTypographySize.Sm" Color="var(--sg-text-muted)">
                Configure every parameter interactively and see the result with generated code you can copy.
            </SgTypography>
        </div>

        <SgCard Shadow="SgShadow.Md" Radius="SgSize.Lg" NoPadding="true">
            <SgRow Space="SgSize.Md">

                <!-- ── Control Panel ── -->
                <SgCol Xs="12" Md="5" Style="padding:24px;border-right:1px solid var(--sg-border);">
                    <SgStack Vertical Space="SgSize.Lg">

                        <!-- Size -->
                        <SgStack Vertical Space="SgSize.Sm">
                            <SgTypography Size="SgTypographySize.Xs" Weight="SgTypographyWeight.Bold" Transform="SgTypographyTransform.Uppercase" Tracking="SgTypographyTracking.Wide" Color="var(--sg-text-muted)">Size</SgTypography>
                            <SgSegmented TValue="SgSize"
                                         Value="@_ctorSize"
                                         ValueChanged="v => { _ctorSize = v; UpdateCtorCode(); }"
                                         Block="true"
                                         Size="sm"
                                         Options="_sizeSegOptions" />
                        </SgStack>

                        <!-- Variant -->
                        <SgStack Vertical Space="SgSize.Sm">
                            <SgTypography Size="SgTypographySize.Xs" Weight="SgTypographyWeight.Bold" Transform="SgTypographyTransform.Uppercase" Tracking="SgTypographyTracking.Wide" Color="var(--sg-text-muted)">Variant</SgTypography>
                            <SgSegmented TValue="SgSpinnerVariant"
                                         Value="@_ctorVariant"
                                         ValueChanged="v => { _ctorVariant = v; UpdateCtorCode(); }"
                                         Block="true"
                                         Size="sm"
                                         Options="_variantSegOptions" />
                        </SgStack>

                        <!-- Value Slider -->
                        <SgStack Vertical Space="SgSize.Sm">
                            <SgTypography Size="SgTypographySize.Xs" Weight="SgTypographyWeight.Bold" Transform="SgTypographyTransform.Uppercase" Tracking="SgTypographyTracking.Wide" Color="var(--sg-text-muted)">
                                Value: <SgBadge Variant="SgBadgeVariant.Info" Text="@_ctorValue.ToString("F0")" Standalone="true" />
                            </SgTypography>
                            <input type="range" min="0" max="100" step="1"
                                   value="@_ctorValue"
                                   @oninput="OnCtorValueChanged"
                                   style="width:100%;accent-color:var(--sg-primary);" />
                        </SgStack>

                        <!-- Toggles row 1 -->
                        <SgRow Space="SgSize.Sm">
                            <SgCol Span="6">
                                <SgStack Vertical Space="SgSize.Sm">
                                    <SgTypography Size="SgTypographySize.Xs" Weight="SgTypographyWeight.Bold" Transform="SgTypographyTransform.Uppercase" Tracking="SgTypographyTracking.Wide" Color="var(--sg-text-muted)">Gradient</SgTypography>
                                    <SgSegmented TValue="bool"
                                                 Value="_ctorGradient"
                                                 ValueChanged="v => { _ctorGradient = v; UpdateCtorCode(); }"
                                                 Block="true"
                                                 Size="sm"
                                                 Options="_boolSegOptions" />
                                </SgStack>
                            </SgCol>
                            <SgCol Span="6">
                                <SgStack Vertical Space="SgSize.Sm">
                                    <SgTypography Size="SgTypographySize.Xs" Weight="SgTypographyWeight.Bold" Transform="SgTypographyTransform.Uppercase" Tracking="SgTypographyTracking.Wide" Color="var(--sg-text-muted)">Glow</SgTypography>
                                    <SgSegmented TValue="bool"
                                                 Value="_ctorGlow"
                                                 ValueChanged="v => { _ctorGlow = v; UpdateCtorCode(); }"
                                                 Block="true"
                                                 Size="sm"
                                                 Options="_boolSegOptions" />
                                </SgStack>
                            </SgCol>
                        </SgRow>

                        <!-- Toggles row 2 -->
                        <SgRow Space="SgSize.Sm">
                            <SgCol Span="6">
                                <SgStack Vertical Space="SgSize.Sm">
                                    <SgTypography Size="SgTypographySize.Xs" Weight="SgTypographyWeight.Bold" Transform="SgTypographyTransform.Uppercase" Tracking="SgTypographyTracking.Wide" Color="var(--sg-text-muted)">Striped</SgTypography>
                                    <SgSegmented TValue="bool"
                                                 Value="_ctorStriped"
                                                 ValueChanged="v => { _ctorStriped = v; UpdateCtorCode(); }"
                                                 Block="true"
                                                 Size="sm"
                                                 Options="_boolSegOptions" />
                                </SgStack>
                            </SgCol>
                            <SgCol Span="6">
                                <SgStack Vertical Space="SgSize.Sm">
                                    <SgTypography Size="SgTypographySize.Xs" Weight="SgTypographyWeight.Bold" Transform="SgTypographyTransform.Uppercase" Tracking="SgTypographyTracking.Wide" Color="var(--sg-text-muted)">Animated</SgTypography>
                                    <SgSegmented TValue="bool"
                                                 Value="_ctorAnimated"
                                                 ValueChanged="v => { _ctorAnimated = v; UpdateCtorCode(); }"
                                                 Block="true"
                                                 Size="sm"
                                                 Options="_boolSegOptions" />
                                </SgStack>
                            </SgCol>
                        </SgRow>

                        <!-- Toggles row 3 -->
                        <SgRow Space="SgSize.Sm">
                            <SgCol Span="6">
                                <SgStack Vertical Space="SgSize.Sm">
                                    <SgTypography Size="SgTypographySize.Xs" Weight="SgTypographyWeight.Bold" Transform="SgTypographyTransform.Uppercase" Tracking="SgTypographyTracking.Wide" Color="var(--sg-text-muted)">Circular</SgTypography>
                                    <SgSegmented TValue="bool"
                                                 Value="_ctorCircular"
                                                 ValueChanged="v => { _ctorCircular = v; if (_ctorCircular) { _ctorVertical = false; } UpdateCtorCode(); }"
                                                 Block="true"
                                                 Size="sm"
                                                 Options="_boolSegOptions" />
                                </SgStack>
                            </SgCol>
                            <SgCol Span="6">
                                <SgStack Vertical Space="SgSize.Sm">
                                    <SgTypography Size="SgTypographySize.Xs" Weight="SgTypographyWeight.Bold" Transform="SgTypographyTransform.Uppercase" Tracking="SgTypographyTracking.Wide" Color="var(--sg-text-muted)">Vertical</SgTypography>
                                    <SgSegmented TValue="bool"
                                                 Value="_ctorVertical"
                                                 ValueChanged="v => { _ctorVertical = v; if (_ctorVertical) { _ctorCircular = false; } UpdateCtorCode(); }"
                                                 Block="true"
                                                 Size="sm"
                                                 Options="_boolSegOptions" />
                                </SgStack>
                            </SgCol>
                        </SgRow>

                        <!-- Toggles row 4 -->
                        <SgRow Space="SgSize.Sm">
                            <SgCol Span="6">
                                <SgStack Vertical Space="SgSize.Sm">
                                    <SgTypography Size="SgTypographySize.Xs" Weight="SgTypographyWeight.Bold" Transform="SgTypographyTransform.Uppercase" Tracking="SgTypographyTracking.Wide" Color="var(--sg-text-muted)">ShowLabel</SgTypography>
                                    <SgSegmented TValue="bool"
                                                 Value="_ctorShowLabel"
                                                 ValueChanged="v => { _ctorShowLabel = v; UpdateCtorCode(); }"
                                                 Block="true"
                                                 Size="sm"
                                                 Options="_boolSegOptionsOn" />
                                </SgStack>
                            </SgCol>
                            <SgCol Span="6">
                                <SgStack Vertical Space="SgSize.Sm">
                                    <SgTypography Size="SgTypographySize.Xs" Weight="SgTypographyWeight.Bold" Transform="SgTypographyTransform.Uppercase" Tracking="SgTypographyTracking.Wide" Color="var(--sg-text-muted)">ShowAnimation</SgTypography>
                                    <SgSegmented TValue="bool"
                                                 Value="_ctorShowAnimation"
                                                 ValueChanged="v => { _ctorShowAnimation = v; UpdateCtorCode(); }"
                                                 Block="true"
                                                 Size="sm"
                                                 Options="_boolSegOptions" />
                                </SgStack>
                            </SgCol>
                        </SgRow>

                        <!-- Summary -->
                        <div class="ctrl-summary">
                            <SgStack Vertical Space="SgSize.Sm">
                                <SgStack Horizontal Space="SgSize.Sm" Align="SgAlignItems.Center" Justify="SgJustifyContent.SpaceBetween">
                                    <SgTypography Size="SgTypographySize.Sm" Color="var(--sg-text-muted)">Size</SgTypography>
                                    <SgBadge Variant="SgBadgeVariant.Info" Text="@_ctorSize.ToString()" Standalone="true" />
                                </SgStack>
                                <SgStack Horizontal Space="SgSize.Sm" Align="SgAlignItems.Center" Justify="SgJustifyContent.SpaceBetween">
                                    <SgTypography Size="SgTypographySize.Sm" Color="var(--sg-text-muted)">Variant</SgTypography>
                                    <SgBadge Variant="SgBadgeVariant.Success" Text="@_ctorVariant.ToString()" Standalone="true" />
                                </SgStack>
                                <SgStack Horizontal Space="SgSize.Sm" Align="SgAlignItems.Center" Justify="SgJustifyContent.SpaceBetween">
                                    <SgTypography Size="SgTypographySize.Sm" Color="var(--sg-text-muted)">Value</SgTypography>
                                     <SgBadge Variant="SgBadgeVariant.Default" Text="@GetCtorValueLabel()" Standalone="true" />
                                </SgStack>
                                <SgStack Horizontal Space="SgSize.Sm" Align="SgAlignItems.Center" Justify="SgJustifyContent.SpaceBetween">
                                    <SgTypography Size="SgTypographySize.Sm" Color="var(--sg-text-muted)">Mode</SgTypography>
                                    <SgBadge Variant="SgBadgeVariant.Default" Text="@GetCtorModeLabel()" Standalone="true" />
                                </SgStack>
                                <SgStack Horizontal Space="SgSize.Sm" Align="SgAlignItems.Center" Justify="SgJustifyContent.SpaceBetween">
                                    <SgTypography Size="SgTypographySize.Sm" Color="var(--sg-text-muted)">Effects</SgTypography>
                                    <SgStack Horizontal Space="SgSize.Sm">
                                        @if (_ctorGradient) { <SgBadge Variant="SgBadgeVariant.Info" Text="Grad" Standalone="true" Size="SgSize.Sm" /> }
                                        @if (_ctorGlow) { <SgBadge Variant="SgBadgeVariant.Warn" Text="Glow" Standalone="true" Size="SgSize.Sm" /> }
                                        @if (_ctorStriped) { <SgBadge Variant="SgBadgeVariant.Default" Text="Striped" Standalone="true" Size="SgSize.Sm" /> }
                                        @if (_ctorAnimated) { <SgBadge Variant="SgBadgeVariant.Success" Text="Animated" Standalone="true" Size="SgSize.Sm" /> }
                                        @if (!_ctorGradient && !_ctorGlow && !_ctorStriped && !_ctorAnimated) { <span style="font-size:12px;color:var(--sg-text-muted);">None</span> }
                                    </SgStack>
                                </SgStack>
                            </SgStack>
                        </div>

                    </SgStack>
                </SgCol>

                <!-- ── Preview + Code ── -->
                <SgCol Xs="12" Md="7" Style="padding:24px;">
                    <SgStack Vertical Space="SgSize.Lg">

                        <!-- Preview -->
                        <SgStack Vertical Space="SgSize.Sm">
                            <div style="display:flex;align-items:center;justify-content:space-between;">
                                <SgTypography Size="SgTypographySize.Xs" Weight="SgTypographyWeight.Bold" Transform="SgTypographyTransform.Uppercase" Tracking="SgTypographyTracking.Wide" Color="var(--sg-text-muted)">Result</SgTypography>
                            </div>
                            <div class="constructor-preview" id="ctor-preview" style="display:flex;align-items:center;justify-content:center;padding:32px;">
                                @if (!_ctorCircular && !_ctorVertical)
                                {
                                    <div style="width:100%;max-width:400px;">
                                        <SgProgress Value="@_ctorValue"
                                                     Size="@_ctorSize"
                                                     Variant="@_ctorVariant"
                                                     Gradient="@_ctorGradient"
                                                     Glow="@_ctorGlow"
                                                     Striped="@_ctorStriped"
                                                     Animated="@_ctorAnimated"
                                                     ShowLabel="@_ctorShowLabel"
                                                     ShowAnimation="@_ctorShowAnimation"
                                                     Title="Live Preview" />
                                    </div>
                                }
                                else if (_ctorCircular)
                                {
                                    <SgProgress Value="@_ctorValue"
                                                 Size="@_ctorSize"
                                                 Variant="@_ctorVariant"
                                                 Gradient="@_ctorGradient"
                                                 Glow="@_ctorGlow"
                                                 Circular="true"
                                                 ShowLabel="@_ctorShowLabel"
                                                 ShowAnimation="@_ctorShowAnimation" />
                                }
                                else if (_ctorVertical)
                                {
                                    <SgProgress Value="@_ctorValue"
                                                 Size="@_ctorSize"
                                                 Variant="@_ctorVariant"
                                                 Gradient="@_ctorGradient"
                                                 Glow="@_ctorGlow"
                                                 Striped="@_ctorStriped"
                                                 Animated="@_ctorAnimated"
                                                 Vertical="true"
                                                 ShowLabel="@_ctorShowLabel"
                                                 ShowAnimation="@_ctorShowAnimation" />
                                }
                            </div>
                        </SgStack>

                        <!-- Code with Copy -->
                        <SgStack Vertical Space="SgSize.Sm">
                            <div style="display:flex;align-items:center;justify-content:space-between;">
                                <SgTypography Size="SgTypographySize.Xs" Weight="SgTypographyWeight.Bold" Transform="SgTypographyTransform.Uppercase" Tracking="SgTypographyTracking.Wide" Color="var(--sg-text-muted)">Generated Code</SgTypography>
                                <button type="button" class="demo-copy-btn" title="Copy to clipboard" @onclick="CopyCtorCode">
                                    <span id="copy-icon">@_copyIcon</span>
                                    <span id="copy-text" style="font-size:11px;font-weight:600;">@_copyLabel</span>
                                </button>
                            </div>
                            <div class="code-block" id="ctor-code-block">
                                @foreach (var line in _ctorCodeLines)
                                {
                                    <code>@line</code>
                                }
                            </div>
                        </SgStack>

                    </SgStack>
                </SgCol>

            </SgRow>
        </SgCard>
    </div>

</ChildContent>
</SgCard>

<style>
    /* ── Section layout ─────────────────────────────────────────────────────── */
    .demo-section {
        margin-top: 48px;
    }
    .demo-section-header {
        margin-bottom: 20px;
    }

    .demo-two-col {
        display: grid;
        grid-template-columns: 1fr 1fr;
        gap: 24px;
    }
    @@media (max-width: 768px) {
        .demo-two-col { grid-template-columns: 1fr; }
    }

    /* ── Constructor ────────────────────────────────────────────────────────── */
    .constructor-preview {
        background: var(--sg-surface-alt);
        border: 1px solid var(--sg-border);
        border-radius: 8px;
        padding: 16px;
        min-height: 100px;
        transition: all 0.3s ease;
    }
    .ctrl-summary {
        background: var(--sg-surface-alt);
        border: 1px solid var(--sg-border);
        border-radius: 8px;
        padding: 12px 14px;
    }
    .code-block {
        background: #0f172a;
        color: #7dd3fc;
        padding: 14px 16px;
        border-radius: 8px;
        font-family: 'JetBrains Mono', 'Fira Code', monospace;
        font-size: 12px;
        line-height: 1.9;
        overflow-x: auto;
        position: relative;
    }
    .code-block code {
        background: none;
        padding: 0;
        color: inherit;
        font-size: inherit;
        display: block;
        white-space: pre;
    }

    /* ── Copy button ────────────────────────────────────────────────────────── */
    .demo-copy-btn {
        display: inline-flex;
        align-items: center;
        gap: 4px;
        background: var(--sg-surface-alt);
        border: 1px solid var(--sg-border);
        border-radius: 6px;
        padding: 4px 10px;
        cursor: pointer;
        color: var(--sg-text-muted);
        transition: all 0.2s ease;
        font-family: inherit;
    }
    .demo-copy-btn:hover {
        color: var(--sg-primary);
        border-color: var(--sg-primary);
        background: color-mix(in srgb, var(--sg-primary) 8%, transparent);
    }
    .demo-copy-btn:active {
        transform: scale(0.95);
    }
    .demo-copy-btn.copied {
        color: var(--sg-color-success);
        border-color: var(--sg-color-success);
    }

    /* ── Range slider ───────────────────────────────────────────────────────── */
    input[type="range"] {
        -webkit-appearance: none;
        appearance: none;
        height: 6px;
        border-radius: 3px;
        background: var(--sg-surface-alt);
        outline: none;
        border: 1px solid var(--sg-border);
    }
    input[type="range"]::-webkit-slider-thumb {
        -webkit-appearance: none;
        appearance: none;
        width: 18px;
        height: 18px;
        border-radius: 50%;
        background: var(--sg-primary);
        cursor: pointer;
        border: 2px solid #fff;
        box-shadow: 0 1px 4px rgba(0,0,0,0.2);
    }
    input[type="range"]::-moz-range-thumb {
        width: 18px;
        height: 18px;
        border-radius: 50%;
        background: var(--sg-primary);
        cursor: pointer;
        border: 2px solid #fff;
    }
</style>

@code {
    // ═══════════════════════════════════════════════════════════════════════════
    //                              MAIN CARD HEADER
    // ═══════════════════════════════════════════════════════════════════════════

    private RenderFragment _mainHeader => __builder =>
    {
        <SgStack Horizontal Space="SgSize.Md" Align="SgAlignItems.Center">
            <div style="display:flex;align-items:center;justify-content:center;width:40px;height:40px;border-radius:10px;background:linear-gradient(135deg,var(--sg-primary),#8b5cf6);color:#fff;flex-shrink:0;">
                <svg viewBox="0 0 24 24" width="20" height="20" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                    <path d="M12 20V10" /><path d="M18 20V4" /><path d="M6 20v-4" />
                </svg>
            </div>
            <SgStack Vertical Space="SgSize.Sm" Align="SgAlignItems.Start">
                <SgTypography Variant="SgTypographyVariant.Heading4" Weight="SgTypographyWeight.Bold">Progress Indicators</SgTypography>
                <SgTypography Size="SgTypographySize.Sm" Color="var(--sg-text-muted)">
                    SgProgress — Linear, circular, segmented, vertical, and indeterminate progress bars with gradients, glow, animated stripes, and custom colors.
                </SgTypography>
            </SgStack>
        </SgStack>
    };

    // ═══════════════════════════════════════════════════════════════════════════
    //                              BUFFER / STREAMING STATE
    // ═══════════════════════════════════════════════════════════════════════════

    private double _videoProgress = 45;
    private double _videoBuffer = 75;
    private double _audioProgress = 62;
    private double _audioBuffer = 88;
    private double _downloadProgress = 0;
    private double _downloadBuffer = 0;
    private Timer? _downloadTimer;

    // ═══════════════════════════════════════════════════════════════════════════
    //                              STICKY TOP STATE
    // ═══════════════════════════════════════════════════════════════════════════

    private bool _showStickyProgress = false;
    private double _stickyProgress = 0;
    private Timer? _stickyTimer;

    // ═══════════════════════════════════════════════════════════════════════════
    //                           CONSTRUCTOR STATE
    // ═══════════════════════════════════════════════════════════════════════════

    private SgSize _ctorSize = SgSize.Lg;
    private SgSpinnerVariant _ctorVariant = SgSpinnerVariant.Info;
    private double _ctorValue = 65;
    private bool _ctorGradient;
    private bool _ctorGlow;
    private bool _ctorStriped;
    private bool _ctorAnimated;
    private bool _ctorCircular;
    private bool _ctorVertical;
    private bool _ctorShowLabel = true;
    private bool _ctorShowAnimation;

    private string[] _ctorCodeLines = Array.Empty<string>();
    private string _copyIcon = "📋";
    private string _copyLabel = "Copy";

    // ═══════════════════════════════════════════════════════════════════════════
    //                              LIFECYCLE
    // ═══════════════════════════════════════════════════════════════════════════

    protected override void OnInitialized()
    {
        var bufferTimer = new Timer(_ =>
        {
            InvokeAsync(() =>
            {
                _videoProgress = Math.Min(100, _videoProgress + Random.Shared.Next(1, 3));
                _videoBuffer = Math.Min(100, _videoProgress + Random.Shared.Next(10, 25));
                _audioProgress = Math.Min(100, _audioProgress + Random.Shared.Next(1, 4));
                _audioBuffer = Math.Min(100, _audioProgress + Random.Shared.Next(15, 30));
                if (_videoProgress >= 100) _videoProgress = 0;
                if (_audioProgress >= 100) _audioProgress = 0;
                StateHasChanged();
            });
        }, null, TimeSpan.FromMilliseconds(500), TimeSpan.FromMilliseconds(500));

        UpdateCtorCode();
    }

    public void Dispose()
    {
        _downloadTimer?.Dispose();
        _stickyTimer?.Dispose();
    }

    // ═══════════════════════════════════════════════════════════════════════════
    //                              METHODS
    // ═══════════════════════════════════════════════════════════════════════════

    private void OnCtorValueChanged(ChangeEventArgs e)
    {
        if (double.TryParse(e.Value?.ToString(), out var val))
        {
            _ctorValue = val;
            UpdateCtorCode();
        }
    }

    private void StartDownload()
    {
        _downloadProgress = 0;
        _downloadBuffer = 0;
        _downloadTimer?.Dispose();
        _downloadTimer = new Timer(_ =>
        {
            InvokeAsync(() =>
            {
                if (_downloadProgress < 100)
                {
                    _downloadProgress = Math.Min(100, _downloadProgress + Random.Shared.Next(2, 8));
                    _downloadBuffer = Math.Min(100, _downloadProgress + Random.Shared.Next(5, 15));
                    StateHasChanged();
                }
                else
                {
                    _downloadTimer?.Dispose();
                }
            });
        }, null, TimeSpan.FromMilliseconds(200), TimeSpan.FromMilliseconds(200));
    }

    private void StopDownload()
    {
        _downloadTimer?.Dispose();
    }

    private void SimulatePageLoad()
    {
        _showStickyProgress = true;
        _stickyProgress = 0;
        _stickyTimer?.Dispose();
        _stickyTimer = new Timer(_ =>
        {
            InvokeAsync(() =>
            {
                if (_stickyProgress < 100)
                {
                    if (_stickyProgress < 30)
                        _stickyProgress += Random.Shared.Next(5, 15);
                    else if (_stickyProgress < 70)
                        _stickyProgress += Random.Shared.Next(2, 8);
                    else if (_stickyProgress < 90)
                        _stickyProgress += Random.Shared.Next(1, 4);
                    else
                        _stickyProgress += Random.Shared.Next(1, 3);
                    _stickyProgress = Math.Min(100, _stickyProgress);
                    StateHasChanged();
                }
                else
                {
                    Task.Delay(500).ContinueWith(_ =>
                    {
                        InvokeAsync(() =>
                        {
                            _showStickyProgress = false;
                            StateHasChanged();
                        });
                    });
                    _stickyTimer?.Dispose();
                }
            });
        }, null, TimeSpan.FromMilliseconds(150), TimeSpan.FromMilliseconds(150));
    }

    private string GetCtorValueLabel() => $"{_ctorValue:F0}%";

    private string GetCtorModeLabel()
    {
        if (_ctorCircular) return "Circular";
        if (_ctorVertical) return "Vertical";
        return "Linear";
    }

    // ═══════════════════════════════════════════════════════════════════════════
    //                         CONSTRUCTOR CODE GENERATION
    // ═══════════════════════════════════════════════════════════════════════════

    private void UpdateCtorCode()
    {
        var lines = new List<string>();
        lines.Add("<SgProgress");

        if (_ctorValue != 0)
            lines.Add($"           Value=\"{_ctorValue}\"");

        if (_ctorSize != SgSize.Lg)
            lines.Add($"           Size=\"SgSize.{_ctorSize}\"");

        if (_ctorVariant != SgSpinnerVariant.Default)
            lines.Add($"           Variant=\"SgSpinnerVariant.{_ctorVariant}\"");

        if (_ctorGradient) lines.Add("           Gradient=\"true\"");
        if (_ctorGlow) lines.Add("           Glow=\"true\"");
        if (_ctorStriped) lines.Add("           Striped=\"true\"");
        if (_ctorAnimated) lines.Add("           Animated=\"true\"");
        if (_ctorCircular) lines.Add("           Circular=\"true\"");
        if (_ctorVertical) lines.Add("           Vertical=\"true\"");
        if (!_ctorShowLabel) lines.Add("           ShowLabel=\"false\"");
        if (_ctorShowAnimation) lines.Add("           ShowAnimation=\"true\"");

        lines.Add("           />");
        _ctorCodeLines = lines.ToArray();
    }

    private async Task CopyCtorCode()
    {
        var code = string.Join("\n", _ctorCodeLines);
        try
        {
            await JS.InvokeVoidAsync("navigator.clipboard.writeText", code);
            _copyIcon = "✓";
            _copyLabel = "Copied!";
            StateHasChanged();
            await Task.Delay(2000);
            _copyIcon = "📋";
            _copyLabel = "Copy";
            StateHasChanged();
        }
        catch
        {
            _copyIcon = "✗";
            _copyLabel = "Failed";
            StateHasChanged();
            await Task.Delay(1500);
            _copyIcon = "📋";
            _copyLabel = "Copy";
            StateHasChanged();
        }
    }

    // ═══════════════════════════════════════════════════════════════════════════
    //                         SEGMENTED OPTIONS
    // ═══════════════════════════════════════════════════════════════════════════

    private readonly List<SgSegmentedOption<SgSize>> _sizeSegOptions = new()
    {
        new() { Value = SgSize.Sm, Label = "Sm" },
        new() { Value = SgSize.Md, Label = "Md" },
        new() { Value = SgSize.Lg, Label = "Lg" },
        new() { Value = SgSize.Xl, Label = "Xl" },
    };

    private readonly List<SgSegmentedOption<SgSpinnerVariant>> _variantSegOptions = new()
    {
        new() { Value = SgSpinnerVariant.Default, Label = "Default" },
        new() { Value = SgSpinnerVariant.Info,    Label = "Info" },
        new() { Value = SgSpinnerVariant.Success, Label = "Success" },
        new() { Value = SgSpinnerVariant.Warn,    Label = "Warn" },
        new() { Value = SgSpinnerVariant.Danger,  Label = "Danger" },
    };

    private readonly List<SgSegmentedOption<bool>> _boolSegOptions = new()
    {
        new() { Value = false, Label = "Off" },
        new() { Value = true,  Label = "On" },
    };

    private readonly List<SgSegmentedOption<bool>> _boolSegOptionsOn = new()
    {
        new() { Value = true,  Label = "On" },
        new() { Value = false, Label = "Off" },
    };

    // ═══════════════════════════════════════════════════════════════════════════
    //                              PROPERTY TABLES
    // ═══════════════════════════════════════════════════════════════════════════

    private List<PropertyPanelItem> _progressProperties = new()
    {
        new() { Label = "Value", Value = "double", BadgeText = "Current progress (0 to Max)", BadgeVariant = SgBadgeVariant.Info },
        new() { Label = "Max", Value = "double", BadgeText = "Maximum value (default 100)", BadgeVariant = SgBadgeVariant.Info },
        new() { Label = "ShowLabel", Value = "bool", BadgeText = "Show percentage (default true)", BadgeVariant = SgBadgeVariant.Default },
        new() { Label = "Size", Value = "SgSize", BadgeText = "Sm, Md, Lg, Xl", BadgeVariant = SgBadgeVariant.Success },
        new() { Label = "Variant", Value = "SgSpinnerVariant", BadgeText = "Color variant", BadgeVariant = SgBadgeVariant.Info },
        new() { Label = "Title", Value = "string?", BadgeText = "Header title", BadgeVariant = SgBadgeVariant.Default },
        new() { Label = "Description", Value = "string?", BadgeText = "Text below bar", BadgeVariant = SgBadgeVariant.Default },
        new() { Label = "Gradient", Value = "bool", BadgeText = "Gradient fill", BadgeVariant = SgBadgeVariant.Warn },
        new() { Label = "Glow", Value = "bool", BadgeText = "Glow effect", BadgeVariant = SgBadgeVariant.Warn },
        new() { Label = "Striped", Value = "bool", BadgeText = "Diagonal stripes", BadgeVariant = SgBadgeVariant.Default },
        new() { Label = "Animated", Value = "bool", BadgeText = "Animate stripes", BadgeVariant = SgBadgeVariant.Default },
        new() { Label = "ShowAnimation", Value = "bool", BadgeText = "Animate value changes", BadgeVariant = SgBadgeVariant.Default },
    };

    private List<PropertyPanelItem> _circularProperties = new()
    {
        new() { Label = "Circular", Value = "bool", BadgeText = "Circular indicator", BadgeVariant = SgBadgeVariant.Info },
        new() { Label = "Value", Value = "double", BadgeText = "Current value (0-100)", BadgeVariant = SgBadgeVariant.Info },
        new() { Label = "Size", Value = "SgSize", BadgeText = "Sm(48px) Md(64px) Lg(96px) Xl(128px)", BadgeVariant = SgBadgeVariant.Success },
        new() { Label = "Variant", Value = "SgSpinnerVariant", BadgeText = "Color variant", BadgeVariant = SgBadgeVariant.Info },
        new() { Label = "Gradient", Value = "bool", BadgeText = "Gradient fill", BadgeVariant = SgBadgeVariant.Warn },
        new() { Label = "Glow", Value = "bool", BadgeText = "Glow effect", BadgeVariant = SgBadgeVariant.Warn },
        new() { Label = "ShowLabel", Value = "bool", BadgeText = "Show center label", BadgeVariant = SgBadgeVariant.Default },
        new() { Label = "StrokeWidth", Value = "double?", BadgeText = "Stroke thickness", BadgeVariant = SgBadgeVariant.Default },
    };

    private List<PropertyPanelItem> _segmentedProperties = new()
    {
        new() { Label = "Segmented", Value = "bool", BadgeText = "Enable segmentation", BadgeVariant = SgBadgeVariant.Info },
        new() { Label = "Segments", Value = "int", BadgeText = "Number of segments (default 10)", BadgeVariant = SgBadgeVariant.Info },
        new() { Label = "Value", Value = "double", BadgeText = "Current value", BadgeVariant = SgBadgeVariant.Info },
        new() { Label = "Variant", Value = "SgSpinnerVariant", BadgeText = "Color variant", BadgeVariant = SgBadgeVariant.Success },
    };

    private List<PropertyPanelItem> _bufferProperties = new()
    {
        new() { Label = "BufferValue", Value = "double?", BadgeText = "Buffer/streaming progress", BadgeVariant = SgBadgeVariant.Info },
        new() { Label = "Value", Value = "double", BadgeText = "Current playback progress", BadgeVariant = SgBadgeVariant.Info },
        new() { Label = "Max", Value = "double", BadgeText = "Maximum value", BadgeVariant = SgBadgeVariant.Default },
    };

    private List<PropertyPanelItem> _indeterminateProperties = new()
    {
        new() { Label = "Indeterminate", Value = "bool", BadgeText = "Unknown duration", BadgeVariant = SgBadgeVariant.Info },
        new() { Label = "Circular", Value = "bool", BadgeText = "Circular spinner", BadgeVariant = SgBadgeVariant.Info },
        new() { Label = "Size", Value = "SgSize", BadgeText = "Indicator size", BadgeVariant = SgBadgeVariant.Success },
        new() { Label = "Variant", Value = "SgSpinnerVariant", BadgeText = "Color variant", BadgeVariant = SgBadgeVariant.Info },
    };

    private List<PropertyPanelItem> _verticalProperties = new()
    {
        new() { Label = "Vertical", Value = "bool", BadgeText = "Vertical orientation", BadgeVariant = SgBadgeVariant.Info },
        new() { Label = "Value", Value = "double", BadgeText = "Current value", BadgeVariant = SgBadgeVariant.Info },
        new() { Label = "Size", Value = "SgSize", BadgeText = "Bar width", BadgeVariant = SgBadgeVariant.Success },
        new() { Label = "Variant", Value = "SgSpinnerVariant", BadgeText = "Color variant", BadgeVariant = SgBadgeVariant.Info },
    };

    private List<PropertyPanelItem> _colorProperties = new()
    {
        new() { Label = "Color", Value = "string?", BadgeText = "Custom HEX/RGB/CSS color", BadgeVariant = SgBadgeVariant.Info },
        new() { Label = "Variant", Value = "SgSpinnerVariant", BadgeText = "Ignored when Color is set", BadgeVariant = SgBadgeVariant.Default },
    };

    private List<PropertyPanelItem> _stickyProperties = new()
    {
        new() { Label = "StickyTop", Value = "bool", BadgeText = "Fixed to top of page", BadgeVariant = SgBadgeVariant.Info },
        new() { Label = "ShowLabel", Value = "bool", BadgeText = "Usually false for sticky", BadgeVariant = SgBadgeVariant.Default },
        new() { Label = "Size", Value = "SgSize", BadgeText = "Sm recommended", BadgeVariant = SgBadgeVariant.Success },
        new() { Label = "PulseOnComplete", Value = "bool", BadgeText = "Pulse on finish", BadgeVariant = SgBadgeVariant.Default },
    };
}
